# รายการแก้ไขรวม — จากการทดสอบ UI ครบทั้ง 3 role

> วิธีใช้: วางไฟล์นี้ในโฟลเดอร์โปรเจกต์ แล้วสั่ง Claude Code:
> "อ่าน FIXLIST_all_roles.md แล้วแก้ทีละกลุ่ม เริ่มกลุ่ม A หยุดรายงานให้ฉันตรวจก่อนไปกลุ่มถัดไป"
>
> **บังคับกฎที่ backend เสมอ ไม่ใช่แค่ซ่อนปุ่มใน UI** และเขียน pytest คุมทุกข้อ

---

# กลุ่ม A — ความถูกต้องของข้อมูลชั่วโมง 🔴 สำคัญที่สุด

*(นี่คือแก่นของโครงงาน — ปัญหาที่ข้อเสนอระบุว่าจะแก้ คือความคลาดเคลื่อนของชั่วโมงกิจกรรม)*

**A1. เพิ่มฟิลด์ `hours` ในตาราง activity**
- กิจกรรมต้องกำหนดได้ว่าให้กี่ชั่วโมง (เช่น ปฐมนิเทศ = 4 ชม.)
- เพิ่มในฟอร์มสร้าง/แก้ไขกิจกรรม (staff/admin กรอก)
- Alembic migration + backfill ค่าให้กิจกรรมเดิม

**A2. ห้ามพิมพ์ `hours_earned` เอง**
- ตอนนี้ staff/admin พิมพ์ตัวเลขชั่วโมงเองได้อิสระในหน้าต่าง "แก้ไขการเข้าร่วม" → **ต้องเอาช่องนี้ออก**
- ให้ระบบดึงชั่วโมงจาก `activity.hours` อัตโนมัติ **ตอนที่อนุมัติหลักฐาน**
- backend ต้องปฏิเสธถ้ามีการส่ง `hours_earned` มาเอง

**A3. ห้ามอนุมัติถ้ายังไม่มีหลักฐาน**
- เปลี่ยน `evidence_status` เป็น `approved` ได้เฉพาะเมื่อ `has_evidence = true` → ถ้าไม่มี ให้ตอบ 400
- ตอนนี้อนุมัติได้ทั้งที่ทุกแถวขึ้น "ยังไม่มีหลักฐาน" ซึ่งทำลายเหตุผลของ workflow หลักฐานทั้งหมด

**A4. ห้ามเปลี่ยนนิสิต/กิจกรรมของรายการที่มีอยู่แล้ว**
- หน้าต่างแก้ไขการเข้าร่วมเปลี่ยน `student_id` และ `activity_id` ได้ → **ต้องล็อกไว้** (แสดงเป็นข้อความอ่านอย่างเดียว)
- backend ปฏิเสธถ้าส่งค่าใหม่มา; ถ้าจะแก้จริงให้ลบแล้วสร้างใหม่

**pytest:** ส่ง hours_earned เอง → ถูกปฏิเสธ · อนุมัติทั้งที่ไม่มีหลักฐาน → 400 · แก้ student_id/activity_id → ถูกปฏิเสธ · อนุมัติแล้วชั่วโมงเท่ากับ activity.hours

---

# กลุ่ม B — หลักฐาน / Bronze Layer 🔴

**B1. แก้บั๊ก `ImageCodecException` — เปิดดูรูปหลักฐานไม่ได้**
```
Failed to decode image using the browser's ImageDecoder API
InvalidStateError: Failed to retrieve track metadata
```
- `GET /participations/{id}/evidence` ต้องส่ง **Content-Type ที่ถูกต้อง** (`image/jpeg`, `image/png`, `application/pdf`) และส่ง **raw bytes** ตรง ๆ
- ตรวจว่าไม่ได้ห่อเป็น JSON หรือส่ง URL กลับมาแทนไฟล์
- **นี่คือบั๊กที่หนักที่สุด** เพราะทำให้ฟีเจอร์หลักของ Bronze Layer ใช้งานไม่ได้จริง

**B2. อนุญาตอัปโหลดหลักฐานใหม่เมื่อถูกปฏิเสธ**
- ตอนนี้แถวที่ `rejected` ไม่มีปุ่มอัปโหลด นิสิตแก้ตัวไม่ได้เลย
- ให้อัปโหลดใหม่ได้เมื่อสถานะเป็น `pending` **หรือ** `rejected` (ถ้า `approved` → 400)

**B3. ซ่อนปุ่มดูหลักฐานเมื่อไม่มีไฟล์**
- ปุ่ม 🖼️ แสดงอยู่แม้แถวจะขึ้น "ยังไม่มีหลักฐาน" → ซ่อน/ปิดใช้งานเมื่อ `has_evidence = false`

**pytest:** อัปโหลดทับตอน rejected → สำเร็จ · ตอน approved → 400 · GET evidence ต้องได้ Content-Type ตรงชนิดไฟล์

---

# กลุ่ม C — Ownership ของ staff 🔴

**C1. ยืนยันว่า staff เห็นเฉพาะกิจกรรมของตัวเองจริง**
- ตอนนี้ **staff เห็นครบ 10 กิจกรรม และ 50 การเข้าร่วม เท่ากับ admin เป๊ะ** → สงสัยว่า filter ไม่ทำงาน (หรือ seed ตั้ง created_by เป็น staff ทั้งหมด ซึ่งไม่ได้พิสูจน์อะไร)
- **ทดสอบจริง:** ให้ admin สร้างกิจกรรมใหม่ → login เป็น staff → ต้องมองไม่เห็นกิจกรรมนั้น
- แก้ seed ให้กิจกรรมกระจายเจ้าของ (บางอันเป็นของ staff, บางอันของ admin) จะได้ทดสอบได้จริง

**pytest:** staff เห็นเฉพาะ activity ที่ `created_by = ตัวเอง` · เปิดผู้เข้าร่วมของกิจกรรมคนอื่น → 403 · GET /participations คืนเฉพาะที่อยู่ในกิจกรรมของตัวเอง

---

# กลุ่ม D — จัดการผู้ใช้ (admin) 🔴

**D1. ฟอร์ม "เพิ่มผู้ใช้" ต้องผูกกับนิสิตได้**
- ตอนนี้มีแค่ ชื่อผู้ใช้ / รหัสผ่าน / สิทธิ์ — **ขาดช่องเลือกนิสิต (`student_id`)**
- ถ้าสร้าง user role = student โดยไม่ผูก **คนนั้นจะ login ได้แต่ไม่เห็นข้อมูลตัวเองเลย** (row-level access อาศัย `user.student_id`)
- ให้แสดง dropdown เลือกนิสิตเมื่อเลือก role = นิสิต และบังคับกรอก

**D2. แก้ไข/ลบผู้ใช้ได้**
- ตอนนี้มีแค่ปุ่มเพิ่ม → admin เปลี่ยน role ไม่ได้ รีเซ็ตรหัสผ่านไม่ได้ ลบผู้ใช้ไม่ได้
- เพิ่ม `PUT /auth/users/{id}` (เปลี่ยน role / รีเซ็ตรหัสผ่าน) และ `DELETE /auth/users/{id}` — **admin เท่านั้น**
- กันไม่ให้ admin ลบตัวเอง

**pytest:** staff เรียก PUT/DELETE users → 403 · admin ลบตัวเอง → 400 · สร้าง user นิสิตพร้อม student_id แล้ว login เห็นข้อมูลตัวเองได้

---

# กลุ่ม E — UI / UX 🟡

**E1. ชื่อกิจกรรมขึ้นเป็น `#1`, `#2`** (ฝั่งนิสิต)
- ให้ `ParticipationRead` คืน `activity_name` มาด้วย — ตอนนี้ frontend resolve ชื่อเองไม่ได้เพราะกิจกรรม pending ไม่อยู่ในลิสต์ที่นิสิตดึงได้

**E2. คำว่า "จัดการ" ในมุมมองนิสิต**
- หัวข้อขึ้น "**จัดการ**กิจกรรม" และการ์ดเขียน "**จัดการ**ข้อมูลกิจกรรม" ทั้งที่นิสิตดูได้อย่างเดียว
- เปลี่ยนเป็น "กิจกรรม" / "ดูกิจกรรมทั้งหมด" เมื่อ role = student

**E3. หน้าต่างแก้ไขกิจกรรม — ช่องหมวดชั่วโมงพัง**
- ข้อความยาวล้นออกนอกกรอบ dialog และยังไม่ใช่ dropdown (ต่างจากช่อง "ประเภท" ที่มีลูกศรเลือกได้)
- ทำเป็น **dropdown เลือก subcategory** แสดงชื่อสั้น และไม่ให้ข้อความล้น

**E4. แถวที่ "ไม่อนุมัติ" ยังโชว์ชั่วโมง** (เช่น 6, 1.8) → ควรแสดง 0 หรือ "–" เพื่อไม่ให้สับสน

**E5. ปุ่ม "Back" ซ้ำ** มุมซ้ายบนหน้าจัดการนิสิต/กิจกรรม (มีลูกศร ← อยู่แล้ว) → ลบออก *(แจ้งไปตั้งแต่รอบก่อน ยังไม่ได้แก้)*

---

# กลุ่ม F — ข้อมูล seed 🟡

**F1. ชื่อกิจกรรมไม่สอดคล้องกับประเภท/หมวด**
ตัวอย่างที่ผิดชัดเจน:
- "สัมมนาเตรียมความพร้อมสู่การทำงาน" → ประเภท **กีฬา**
- "จิตอาสาบริจาคโลหิต" → ประเภท **ศิลปวัฒนธรรม**
- "อบรมทักษะดิจิทัล" → ประเภท **จิตอาสา**
- "งานวันวิทยาศาสตร์" → หมวด **ICT กับการรู้สารสนเทศ 1**

ให้แก้ `seed.py` ให้ชื่อกิจกรรม ประเภท และหมวดชั่วโมงสอดคล้องกันตามความหมายจริง — ตอนสอบกรรมการจะสะดุดตาทันที

**F2. ห้ามมีรายการ "อนุมัติแล้ว" ที่ไม่มีหลักฐาน** ใน seed (ขัดกับกฎ A3)

**F3. กระจายเจ้าของกิจกรรม** ให้บางอันเป็นของ staff บางอันของ admin (รองรับการทดสอบ C1)

---

## ลำดับที่แนะนำ

**A → B → C → D → E → F**

กลุ่ม **A, B, C** คือของจริงที่กระทบความน่าเชื่อถือของระบบ ถ้าปิดสามกลุ่มนี้ได้ ระบบจะแน่นพอสำหรับการสอบ ส่วน D–F เป็นความสมบูรณ์และความเรียบร้อย

หลังแก้ครบให้รัน `pytest` ทั้งหมดและรายงานจำนวนเทสต์ที่ผ่าน
