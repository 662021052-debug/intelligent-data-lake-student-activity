# รายการแก้ไข/ฟีเจอร์ รอบ 2 สำหรับ Claude Code

> วิธีใช้: วางไฟล์นี้ในโฟลเดอร์โปรเจกต์ แล้วสั่ง:
> "อ่าน FIXLIST_round2.md แล้วทำทีละข้อ เริ่มข้อ 1 หยุดรายงานให้ฉันตรวจก่อนไปข้อถัดไป"
>
> บังคับกฎที่ backend เสมอ · ไม่แตะ logic/permission เดิม · เขียน/อัปเดต test · แต่ละข้อเสร็จแล้ว commit + push แยก

---

## ข้อ 1 — หน้าจัดการผู้ใช้ (admin) พัง + แก้ไขผู้ใช้ไม่ได้ 🔴

**อาการ:** เปิดหน้าขึ้น "โหลดข้อมูลไม่สำเร็จ" พร้อม error ดิบ `limit ≤ 200 (input 500)` และแก้ไขผู้ใช้ก็ error

1. Frontend ส่ง `limit=500` แต่ backend รับสูงสุด 200 → ทำ **pagination จริง** (โหลดทีละหน้า ~50/หน้า + ปุ่มหน้าถัดไป) ไม่ใช่แค่ลดเลข
2. ตรวจทุกหน้า list (นิสิต/กิจกรรม/ผู้เข้าร่วม/ผู้ใช้) ว่ามีที่ส่ง limit เกิน 200 อีกไหม แก้ให้หมด
3. **แก้ปัญหา "แก้ไขผู้ใช้ไม่ได้"** — ตรวจ `PUT /auth/users/{id}` ว่า error อะไร (มี GET /students/me/หน้าโหลด user พังทำให้แก้ไม่ได้ด้วยไหม) แล้วแก้ให้แก้ไข role/รีเซ็ตรหัส/ผูก student_id ได้จริง
4. **error ให้เป็นมิตร** — เลิกโชว์ JSON ดิบของ Pydantic ให้ผู้ใช้ แสดงข้อความไทยเข้าใจง่ายแทน (ทำเป็นตัวกลางแปลง error ใช้ทั้งแอป)
5. pytest + ทดสอบว่าหน้าโหลดได้และแก้ไขผู้ใช้สำเร็จ

## ข้อ 2 — ตอนสมัครกิจกรรม ต้องบอกหมวดชั่วโมง + จำนวนชั่วโมงที่จะได้ 🔴

ใน dialog ยืนยันสมัครกิจกรรม (และบนการ์ดกิจกรรมที่เปิดรับ) ต้องแสดงชัด:
- **ได้กี่ชั่วโมง** (จาก `activity.hours`)
- **เข้าหมวดไหน** (เช่น "ใฝ่เรียนรู้ตลอดชีวิต › กิจกรรม ICT 1")

เพื่อให้นิสิตรู้ก่อนกดยืนยันว่าสมัครแล้วได้อะไร เข้าหมวดที่ตัวเองยังขาดหรือเปล่า

## ข้อ 3 — OCR ตรวจหลักฐานยังไม่แม่น/ไม่ถูกต้อง 🔴 (ให้วิเคราะห์ก่อนแก้)

**อาการ:** ผลตรวจหลักฐานไม่ตรงกับความจริง

ให้ทำตามลำดับ:
1. **วิเคราะห์ก่อน** — รัน OCR กับรูป seed จริงสัก 5–10 รูป แล้วรายงานว่า: OCR อ่านข้อความออกมาได้อะไร, match_score คิดยังไง, ทำไมตัดสินผิด (ค่า threshold แน่นไป/หลวมไป? การจับคู่คำผิด? รูป seed เป็นภาพ placeholder ที่ไม่มีข้อความจริงให้ OCR อ่าน?)
2. **รายงานให้ฉันดูก่อน** แล้วค่อยเสนอทางแก้ — อย่าเพิ่งแก้มั่ว เพราะสาเหตุอาจเป็นได้หลายอย่าง
3. ทางแก้ที่เป็นไปได้ (เลือกหลังวิเคราะห์): ปรับ threshold, ปรับตรรกะ match_score (เทียบชื่อ/คำสำคัญ/วันที่ให้ยืดหยุ่นขึ้น), หรือถ้ารูป seed ไม่มีข้อความจริง → สร้างรูปหลักฐาน seed ที่มีข้อความจริงให้ OCR อ่านได้
4. **คงหลักการเดิม:** auto-approve เฉพาะที่มั่นใจสูง ที่เหลือส่ง needs_review ให้เจ้าหน้าที่

## ข้อ 4 — ปฏิทินกิจกรรม (ใหม่) 🟡

หน้า **ปฏิทินกิจกรรม** แสดงว่าวันไหนมีกิจกรรมอะไร ดูได้ทั้ง 3 role:
- ใช้ package ปฏิทิน Flutter (เช่น `table_calendar`)
- คลิกวัน → เห็นรายการกิจกรรมของวันนั้น (ชื่อ, เวลา, สถานที่, หมวด, ชั่วโมง)
- **สิทธิ์การมองเห็นตามเดิม:** นิสิตเห็นเฉพาะ approved · staff เห็นของตัวเอง · admin เห็นทั้งหมด (ใช้ endpoint /activities ที่กรองตาม role อยู่แล้ว)
- เพิ่มการ์ด/เมนู "ปฏิทินกิจกรรม" ในหน้า Home ของทั้ง 3 role
- นิสิตกดกิจกรรมในปฏิทิน → ไปสมัครได้เลย (ถ้ายังเปิดรับ)

## ข้อ 5 — ค้นหากิจกรรมแบบ live (พิมพ์แล้วขึ้นเลย) 🟡

ช่องค้นหากิจกรรม (และทุกหน้าที่มีช่องค้นหา) เปลี่ยนจากต้องกด Enter → **ค้นหาขณะพิมพ์**:
- ใช้ `onChanged` + **debounce ~350ms** (อย่ายิง API ทุกตัวอักษร)
- ทำให้สม่ำเสมอทุกหน้า: กิจกรรม / นิสิต / ผู้เข้าร่วม / ผู้ใช้
- *(หมายเหตุ: ถ้าข้อนี้เคยสั่งไปแล้วรอบก่อน ให้ตรวจว่าครบทุกหน้าจริงไหม โดยเฉพาะหน้ากิจกรรม)*

## ข้อ 6 — Dashboard ส่วนตัวของนิสิต (ใหม่) 🟡

ให้นิสิตมีหน้า Dashboard ดูชั่วโมงของตัวเอง (คนละอันกับแดชบอร์ดผู้บริหารที่เป็นภาพรวมทั้งมหาวิทยาลัย):
- ดึงจาก `/students/me/hours-summary`
- **ภาพรวม:** X/60 ชม. + แถบ progress รวม + ผ่านแล้วกี่หมวด
- **รายหมวด:** 5 หมวด แต่ละหมวดมีแถบ earned/required + ไอคอนติ๊กเมื่อครบ (แบบหน้า "ชั่วโมงสะสมของฉัน" ที่มีอยู่ แต่เพิ่มกราฟ/วิชวลให้ดูเป็น dashboard)
- **กราฟ** (fl_chart ตามธีมเดิม): เช่น กราฟแท่ง earned เทียบ required ต่อหมวด
- ปุ่มลัด "แนะนำกิจกรรมที่ยังขาด" (โยงกับ chatbot/หน้าสมัคร)
- *(ถ้าหน้า "ชั่วโมงสะสมของฉัน" เดิมทำหน้าที่นี้อยู่แล้ว ให้ยกระดับหน้าเดิมแทนการสร้างซ้ำ)*

---

## ลำดับแนะนำ
**1 (พัง) → 3 (OCR วิเคราะห์) → 2 → 5 → 6 → 4**
ข้อ 1 เป็นบั๊กที่ทำให้หน้าใช้ไม่ได้ ทำก่อน · ข้อ 3 ให้วิเคราะห์แล้วหยุดรายงานก่อนแก้ · ที่เหลือเป็นฟีเจอร์เสริม

หลังแต่ละข้อ: `flutter analyze` สะอาด + test ผ่าน + commit + push แล้วหยุดให้ฉันตรวจ
