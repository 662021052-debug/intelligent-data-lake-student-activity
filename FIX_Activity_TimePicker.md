# แก้: ตัวเลือกเวลาของกิจกรรมใช้ยาก (Time Picker)

> "อ่าน FIX_Activity_TimePicker.md แล้วแก้ · flutter analyze + test + build web · เสร็จหยุดให้ฉันตรวจ"
> แก้เฉพาะ UX การเลือกเวลา — ไม่แตะ logic การบันทึกกิจกรรม

## ปัญหา
ตอนสร้าง/แก้กิจกรรม การตั้งเวลาใช้ `showTimePicker` แบบหน้าปัดนาฬิกา (analog dial) ซึ่งลากเข็มยาก และมี AM/PM ทำให้สับสน

## สิ่งที่ต้องแก้
หาจุดที่เรียก `showTimePicker(...)` ในฟอร์มกิจกรรม (น่าจะอยู่ใน activities_screen.dart หรือ widget ฟอร์มสร้าง/แก้กิจกรรม) แล้วปรับ 2 อย่าง:

**1. ตัดหน้าปัดนาฬิกาออกทั้งหมด — เหลือช่องพิมพ์เวลาอย่างเดียว**
```dart
initialEntryMode: TimePickerEntryMode.inputOnly,
```
สำคัญ: ต้องเป็น `inputOnly` (ไม่ใช่ `input`) เพื่อ**ไม่ให้มีปุ่มสลับไปหน้าปัด** ผู้ใช้พิมพ์เวลาได้อย่างเดียว ไม่มีนาฬิกาให้ลากเข็มอีก

**2. บังคับรูปแบบ 24 ชั่วโมง (ตัด AM/PM)**
```dart
builder: (context, child) => MediaQuery(
  data: MediaQuery.of(context).copyWith(alwaysUse24HourFormat: true),
  child: child!,
),
```

**3. การแสดงเวลาในฟอร์มและตาราง**
- แสดงเวลาเป็นรูปแบบ 24 ชั่วโมง `HH:mm` (เช่น 09:38, 14:00) ให้สอดคล้องกัน
- ถ้ามีการฟอร์แมต TimeOfDay เป็นข้อความ ใช้ 24 ชม. ไม่ใช่ AM/PM

## ห้ามแตะ
- การประกอบวันเวลา (start_at) ที่ส่งไป backend, การตรวจสอบฟอร์ม, ค่าเริ่มต้นอื่น ๆ

## เกณฑ์ว่าเสร็จ
- [ ] เปิดตัวเลือกเวลาแล้วเป็นช่องพิมพ์เวลาอย่างเดียว **ไม่มีหน้าปัดนาฬิกา และไม่มีปุ่มสลับไปหน้าปัด**
- [ ] เป็นแบบ 24 ชั่วโมง ไม่มี AM/PM
- [ ] เวลาที่บันทึกและแสดงผลถูกต้องตรงกับที่กรอก
- [ ] flutter analyze + test + build web ผ่าน
