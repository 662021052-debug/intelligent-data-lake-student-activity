# สเปกเฟสถัดไปสำหรับ Claude Code — สิทธิ์ตาม role, ownership, สมัครกิจกรรม, หมวดชั่วโมง

> วิธีใช้: วางไฟล์นี้ในโฟลเดอร์โปรเจกต์ แล้วสั่ง Claude Code:
> "อ่าน NEXT_PHASES_for_ClaudeCode.md แล้วทำทีละ Phase เริ่ม Phase 1 หยุดให้ฉันตรวจก่อนไป Phase ถัดไป"
>
> ต่อยอดจากเฟส CRUD เดิม (backend FastAPI + SQLModel + JWT, frontend Flutter Web) ทุก Phase ต้อง **บังคับกฎที่ backend เสมอ ไม่ใช่แค่ซ่อนปุ่มใน frontend** และเขียน pytest คุมทุกกฎ
>
> **หลักการสำคัญ:** staff เป็นเจ้าของกิจกรรมที่ตัวเองเปิด และทำงานได้ **เฉพาะขอบเขตกิจกรรมของตัวเอง** เท่านั้น (ownership model) — ทั้งการจัดการกิจกรรม, ตรวจหลักฐาน, และดูรายชื่อนิสิตที่เข้าร่วม  ส่วนการจัดการข้อมูลนิสิต (master data) เป็นของ **admin เท่านั้น**

---

## Phase 1 — Role-based navigation (จำกัดสิทธิ์ฝั่งนิสิต)

1. หน้า Home: ถ้า role เป็น **student** ไม่แสดงการ์ด "จัดการนิสิต" และเข้าหน้าจัดการนิสิตไม่ได้เลย
2. student: หน้ากิจกรรม = ดูอย่างเดียว ซ่อนปุ่มเพิ่ม/แก้/ลบ; หน้าการเข้าร่วม + ข้อมูลส่วนตัวแสดงเฉพาะของตัวเอง
3. staff/admin: แสดงเมนูจัดการครบตามสิทธิ์
4. Backend enforce: student เรียก POST/PUT/DELETE ใด ๆ → 403; GET /students/{id ของคนอื่น} → 403 หรือ 404
5. pytest: student ดึง/แก้ข้อมูลคนอื่นไม่ได้ (403), เห็นเฉพาะของตัวเอง

## Phase 2 — แยกสิทธิ์ staff / admin + จัดการผู้ใช้ + เก็บงาน UI

1. **admin เท่านั้น**: หน้า "จัดการผู้ใช้" (สร้าง user, กำหนด role) เชื่อมกับ `POST /auth/register`; staff เข้าไม่ได้ — ต้องมีปุ่ม "เพิ่มผู้ใช้ (+)" และฟอร์มกรอก username/password/role จริง
2. **การจัดการข้อมูลนิสิต (master data) = admin เท่านั้น**: เพิ่ม/แก้/ลบนิสิต ทำได้เฉพาะ admin
   - staff **ไม่มี**หน้า "จัดการนิสิต" (roster) และ **แก้ไขข้อมูลนิสิตไม่ได้เลย** (เอาทั้งการ์ดและปุ่มแก้ออกสำหรับ staff)
   - Backend: staff เรียก POST/PUT/DELETE /students → 403
3. เอาปุ่ม "Back" ที่ซ้ำมุมซ้ายบนหน้าจัดการนิสิตออก (มีลูกศร ← อยู่แล้ว)
4. pytest: staff เข้าจัดการผู้ใช้ → 403, staff แก้/ลบนิสิต → 403, admin ทำได้ทั้งหมด

## Phase 3 — เจ้าของกิจกรรม (ownership) + ระบบอนุมัติกิจกรรม

1. เพิ่มฟิลด์ใน activity:
   - `created_by` (user id ของ staff/admin ที่เปิดกิจกรรม)
   - `approval_status` (pending/approved, default = pending), `approved_by`, `approved_at`
2. **ขอบเขต staff = เฉพาะกิจกรรมของตัวเอง**:
   - staff เห็น/แก้/ลบได้เฉพาะกิจกรรมที่ `created_by = ตัวเอง` เท่านั้น (กิจกรรมของ staff คนอื่น → มองไม่เห็น/แก้ไม่ได้ = 403)
   - staff สร้าง/แก้กิจกรรม → บันทึกเป็น **pending** เสมอ
   - admin เห็น/จัดการ **ทุกกิจกรรม**
3. **admin เท่านั้น**: `PATCH /activities/{id}/approve` เปลี่ยนเป็น approved (บันทึก approved_by/at); ปุ่ม "อนุมัติ" แสดงเฉพาะ admin
4. `GET /activities`:
   - student → เห็นเฉพาะ **approved ทั้งหมด** (เพื่อใช้สมัคร)
   - staff → เห็นเฉพาะกิจกรรมของตัวเอง (ทุกสถานะ พร้อม badge pending/approved)
   - admin → เห็นทั้งหมด
5. pytest: staff สร้างกิจกรรม → pending + created_by=ตัวเอง, staff แก้กิจกรรมของคนอื่น → 403, staff เรียก approve → 403, student เห็นเฉพาะ approved

## Phase 4 — ตรวจหลักฐานการเข้าร่วม + ดูรายชื่อนิสิต (ขอบเขตตามเจ้าของกิจกรรม)

1. **staff ตรวจหลักฐาน/อนุมัติชั่วโมงได้เฉพาะการเข้าร่วมของกิจกรรมที่ตัวเองเปิด** (participation ที่ผูกกับ activity.created_by = ตัวเอง); admin ทำได้ทั้งหมด
2. staff เปลี่ยน `evidence_status` (อนุมัติ/ไม่อนุมัติ/รอตรวจสอบ) ได้เฉพาะในขอบเขตของตัวเอง; เมื่ออนุมัติ ชั่วโมงจึงถูกนับ (Phase 6)
3. **staff ดูรายชื่อนิสิตแบบ read-only เฉพาะที่เข้าร่วมกิจกรรมของตัวเอง** (ผ่านหน้าการเข้าร่วม) — ไม่มีสิทธิ์แก้ข้อมูลนิสิต
4. `GET /participations`: student → เฉพาะของตัวเอง; staff → เฉพาะที่อยู่ในกิจกรรมของตัวเอง; admin → ทั้งหมด
5. pytest: staff ตรวจหลักฐานกิจกรรมของตัวเอง → สำเร็จ, ของกิจกรรมคนอื่น → 403, staff แก้ข้อมูลนิสิต → 403
6. คลิกกิจกรรมในหน้าจัดการกิจกรรม → เห็นรายชื่อผู้เข้าร่วมของกิจกรรมนั้น (ชื่อ, เช็กอิน, ชั่วโมง, สถานะหลักฐาน) และตรวจหลักฐานอนุมัติ/ไม่อนุมัติได้ในหน้าเดียวเลย โดย backend มี `GET /activities/{id}/participations` ที่เช็ก ownership ให้ — staff เปิดได้เฉพาะกิจกรรมของตัวเอง ของคนอื่นได้ 403

## Phase 5 — นิสิตสมัครกิจกรรมเอง (self-registration)

1. หน้า Flutter สำหรับนิสิต: "กิจกรรมที่เปิดรับสมัคร" แสดงเฉพาะกิจกรรม **approved + ยังไม่เต็ม + ยังไม่ถึงกำหนด** พร้อมปุ่ม "สมัคร"
2. Backend `POST /participations/register` รับแค่ `activity_id`; server เซ็ต `student_id` จาก JWT, `evidence_status = รอตรวจสอบ`, `hours_earned = 0`
3. Validation: กันสมัครซ้ำ, กิจกรรมต้อง approved, ยังไม่เต็ม (นับจาก max_participants), ยังเปิดรับ
4. นิสิตยกเลิกการสมัครของตัวเองได้ ถ้ายังไม่เช็กอิน/ยังไม่อนุมัติ (ห้ามแตะ record คนอื่น)
5. นิสิต **ห้าม** ส่ง hours_earned / evidence_status เอง — server ต้องปฏิเสธถ้าส่งมา
6. เพิ่มการ์ด "สมัครกิจกรรม" ในหน้า Home ของนิสิต
7. pytest: สมัครของตัวเอง → 201, สมัครให้คนอื่น → 403, สมัครกิจกรรมที่เต็ม → 400, ส่ง hours มาเอง → ถูกปฏิเสธ

## Phase 6 — หมวดชั่วโมงกิจกรรมตามเกณฑ์จริง ม.ทักษิณ (โครงสร้าง พ.ศ. 2560–2566, หลักสูตร 4 ปี, ≥60 ชม.)

1. สร้างตาราง `hour_category` (name, required_hours) และ `hour_subcategory` (category_id, name, required_hours)
2. เพิ่ม `subcategory_id` ใน activity เพื่อผูกกิจกรรมกับหมวดชั่วโมง
3. seed ค่าจริงตามตารางด้านล่าง (ทุกค่าเป็นเกณฑ์ขั้นต่ำ "ไม่น้อยกว่า")
4. Endpoint `GET /students/me/hours-summary`: คืน earned/required แยกตามหมวด + กิจกรรมย่อย โดยนับเฉพาะ participation ที่ `evidence_status = อนุมัติ`
5. หน้า Flutter "ชั่วโมงสะสมของฉัน" (สำหรับนิสิต): แสดง progress แต่ละหมวด + กิจกรรมย่อย + ไอคอนติ๊กเมื่อครบเกณฑ์
6. pytest: การ rollup ชั่วโมงต่อหมวดถูกต้อง, ชั่วโมงที่ยังไม่อนุมัติไม่ถูกนับ

### ข้อมูล seed หมวดชั่วโมง (รวม 60 ชั่วโมง)

| หน่วยการเรียนรู้ (หมวด) | เกณฑ์รวม | กิจกรรมย่อย | เกณฑ์ย่อย |
|---|:---:|---|:---:|
| 1. พัฒนาทักษะชีวิต | 12 | กิจกรรมเสริมสร้างคุณค่าแห่งตน | 8 |
| | | กิจกรรมบูรณาการ 1 | 4 |
| 2. TSU รับใช้สังคม | 12 | กิจกรรม TSU DO D : สร้างสรรค์สร้างสำนึกรับผิดชอบต่อสังคม | 12 |
| 3. ศิลปวัฒนธรรมอาเซียน | 8 | กิจกรรมเรียนรู้ศิลปวัฒนธรรมอาเซียน | 6 |
| | | กิจกรรมบูรณาการ 3 | 2 |
| 4. พัฒนาคุณธรรมและวินัย | 8 | กิจกรรมสร้างวินัยในตนเอง | 4 |
| | | กิจกรรมบูรณาการ 4 | 4 |
| 5. ใฝ่เรียนรู้ตลอดชีวิต | 20 | กิจกรรม ICT กับการรู้สารสนเทศ 1 | 2 |
| | | กิจกรรม ICT กับการรู้สารสนเทศ 2 | 2 |
| | | กิจกรรมเรียนรู้และรู้เท่าทันเทคโนโลยีดิจิทัล | 4 |
| | | กิจกรรมเตรียมความพร้อมก่อนการใช้ชีวิตในมหาวิทยาลัย (ปฐมนิเทศ) | 4 |
| | | กิจกรรมเตรียมความก่อนการใช้ชีวิตนอกมหาวิทยาลัย (ปัจฉิมนิเทศ) | 4 |
| | | กิจกรรมบูรณาการ 5 | 4 |

---

## สรุปตารางสิทธิ์ (permission matrix) — เกณฑ์รวมทุก Phase

| การกระทำ | student | staff | admin |
|---|:---:|:---:|:---:|
| ดูข้อมูล/ชั่วโมงของตัวเอง | ✅ | – | – |
| สมัครกิจกรรมของตัวเอง | ✅ | – | – |
| ดูกิจกรรม | ✅ (approved ทั้งหมด) | ✅ (เฉพาะของตัวเอง ทุกสถานะ) | ✅ (ทั้งหมด) |
| สร้าง/แก้/ลบกิจกรรม | ❌ | ✅ (เฉพาะของตัวเอง → pending) | ✅ (ทั้งหมด) |
| อนุมัติกิจกรรมให้เผยแพร่ | ❌ | ❌ | ✅ |
| ตรวจหลักฐาน/อนุมัติชั่วโมง | ❌ | ✅ (เฉพาะกิจกรรมของตัวเอง) | ✅ (ทั้งหมด) |
| ดูรายชื่อนิสิตที่เข้าร่วม | ❌ | ✅ read-only (เฉพาะกิจกรรมของตัวเอง) | ✅ (ทั้งหมด) |
| เพิ่ม/แก้/ลบข้อมูลนิสิต (master) | ❌ | ❌ | ✅ |
| จัดการผู้ใช้ (สร้าง user/role) | ❌ | ❌ | ✅ |

---

## หมายเหตุ / ต้องเคลียร์กับอาจารย์ก่อน seed จริง

- **กฎการนับชั่วโมง**: ทุกเกณฑ์เป็น "ไม่น้อยกว่า" (ขั้นต่ำ) ต้องถามให้ชัดว่า — นิสิตต้องครบเกณฑ์ขั้นต่ำ **ของกิจกรรมย่อยทุกตัว** หรือแค่ให้ **ยอดรวมของหมวด** ครบก็พอ เพราะมีผลกับการคำนวณและฟีเจอร์ "แนะนำกิจกรรมที่ยังขาด" ของแชตบอตในอนาคต
- ก่อนแก้ schema (Phase 3, 4, 6 เพิ่มคอลัมน์/ตาราง) แนะนำใส่ **Alembic** เพื่อ migrate แทนการลบ dev.db (จำเป็นตอนขึ้น PostgreSQL บนเซิร์ฟเวอร์)
