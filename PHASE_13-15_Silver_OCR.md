# สเปก Phase 13–15 สำหรับ Claude Code — Silver Layer: OCR ตรวจหลักฐานอัตโนมัติ (วัตถุประสงค์ 5.1 + ขอบเขต 6.8)

> วิธีใช้: วางไฟล์นี้ในโฟลเดอร์โปรเจกต์ แล้วสั่ง Claude Code:
> "อ่าน PHASE_13-15_Silver_OCR.md แล้วทำทีละ Phase เริ่ม Phase 13 หยุดให้ฉันตรวจก่อนไป Phase ถัดไป"
>
> ต่อยอดจาก Bronze (MinIO + ตาราง raw_file) · ประมวลผลในเครื่องล้วน (ไม่มีข้อมูลออกนอก) · เขียน pytest คุมทุกส่วน (mock OCR ในเทสต์)

---

## แนวคิด (สำคัญ — อ่านก่อน)

Silver = ชั้น **ทำความสะอาด + ตรวจสอบ + ยกระดับคุณภาพ** ของข้อมูลดิบจาก Bronze

**หลักการที่ต้องยึด — human-in-the-loop:** OCR ภาษาไทยบนรูปถ่าย/เกียรติบัตรมีโอกาสผิดพลาด อย่าให้ระบบ **auto-approve ทุกกรณี** ให้:
- ตรงชัด (confidence สูง) → **auto-approve**
- ไม่ชัด / OCR อ่านไม่ออก / ไม่ตรง → **ตั้งเป็น "รอเจ้าหน้าที่ตรวจ" (needs_review)** ไม่ใช่ปฏิเสธทันที

แบบนี้ตอบกรรมการได้ว่าระบบ **ช่วยลดภาระเจ้าหน้าที่** (ตามข้อเสนอ) โดยไม่เสี่ยงอนุมัติผิด และเป็นการชูจุดเด่นของ Data Lake ในการประมวลผล unstructured data ที่ Data Warehouse เดิมทำไม่ได้

---

# Phase 13 — โครงสร้างพื้นฐาน OCR + Silver table

1. เพิ่มไลบรารี OCR ที่รันในเครื่องและรองรับภาษาไทย: **EasyOCR** (ตั้งง่ายกว่า) หรือ **PaddleOCR**
   - ใน Docker: เพิ่ม dependency + preload โมเดลตอน build image (โมเดลใหญ่ อย่าโหลดตอน runtime ครั้งแรก)
   - รันบน CPU ได้ (ช้าหน่อย) — ไม่บังคับ GPU
2. สร้างตาราง **`silver_evidence_ocr`** (ผลการประมวลผลของแต่ละไฟล์):
   | ฟิลด์ | ความหมาย |
   |---|---|
   | `id` | PK |
   | `raw_file_id` | FK → raw_file (ไฟล์ต้นทางใน Bronze) |
   | `participation_id` | FK → participation |
   | `extracted_text` | ข้อความที่ OCR อ่านได้ทั้งหมด |
   | `ocr_confidence` | ค่าความมั่นใจเฉลี่ยของ OCR (0–1) |
   | `match_score` | คะแนนความตรงกับกิจกรรม/นิสิต (0–1) |
   | `decision` | `auto_approved` / `needs_review` / `flagged` |
   | `is_duplicate` | ตรวจไฟล์ซ้ำจาก checksum ใน raw_file |
   | `processed_at` | เวลา (UTC) |
3. `app/ocr.py`: ฟังก์ชัน `run_ocr(image_bytes) -> (text, confidence)` แยกออกมาให้ mock ได้ในเทสต์
4. pytest (mock `run_ocr`): ประมวลผลแล้วมี row ใน silver_evidence_ocr ครบทุกฟิลด์

# Phase 14 — Pipeline ตรวจสอบ + auto-approval

1. **ทริกเกอร์:** เมื่อมีการอัปโหลดหลักฐาน (Phase 9) ให้ประมวลผล OCR แบบ **background task** (ไม่บล็อกการอัปโหลด) หรือมี endpoint `POST /participations/{id}/evidence/process` ให้ประมวลผลย้อนหลัง
2. **ตรรกะ validation (match_score):** อ่านข้อความจาก OCR แล้วเทียบกับ:
   - ชื่อนิสิต (จาก dim_student) ปรากฏในข้อความไหม
   - คำสำคัญของกิจกรรม / ชื่อกิจกรรม ปรากฏไหม
   - วันที่ในเอกสารใกล้เคียง `activity.start_at` ไหม
   ให้คะแนนรวมเป็น `match_score`
3. **การตัดสินใจ:**
   - `ocr_confidence ≥ 0.6` **และ** `match_score ≥ 0.7` → **auto_approved**: เรียกเส้นทางอนุมัติเดิม (ตั้ง `evidence_status = approved` และ **ดึงชั่วโมงจาก `activity.hours`** ตามกฎ A2 ที่ทำไว้แล้ว) บันทึกว่าใครอนุมัติ = "ระบบ (OCR)"
   - นอกนั้น → **needs_review**: คงสถานะ pending ให้เจ้าหน้าที่ตรวจเอง
   - ไฟล์ซ้ำ (checksum ตรงกับที่เคยอนุมัติ) → **flagged** ไม่ auto-approve
4. **ห้ามข้ามกฎเดิม:** ยังคงกฎ "อนุมัติได้เฉพาะเมื่อมีหลักฐาน" และชั่วโมงมาจาก activity เท่านั้น — OCR แค่มาแทนการกดอนุมัติด้วยมือในกรณีที่ชัดเจน
5. pytest (mock OCR ให้คืนข้อความที่ตรง/ไม่ตรง): เคสตรง → auto_approved + ชั่วโมงเข้าถูกหมวด · เคสไม่ตรง → needs_review (ยัง pending) · ไฟล์ซ้ำ → flagged

# Phase 15 — UI แสดงผล OCR ให้เจ้าหน้าที่

1. หน้า "ผู้เข้าร่วม" (staff): แต่ละแถวที่มีหลักฐาน แสดง:
   - ป้ายผล OCR: ✅ ระบบอนุมัติอัตโนมัติ / 👁️ รอตรวจ / ⚠️ ไฟล์ซ้ำ
   - `match_score` และ `ocr_confidence` (เปอร์เซ็นต์)
2. กดดูรายละเอียด → แสดง **รูปหลักฐาน + ข้อความที่ OCR อ่านได้** เทียบกันข้าง ๆ ให้เจ้าหน้าที่ตัดสินใจง่าย
3. เจ้าหน้าที่ยัง override ได้เสมอ (กดอนุมัติ/ไม่อนุมัติเอง) — auto เป็นตัวช่วย ไม่ใช่ตัวตัดสินสุดท้าย
4. (ถ้ามีเวลา) ปุ่ม "ประมวลผล OCR ทั้งหมดที่ค้าง" สำหรับ admin เพื่อรัน batch

---

## เกณฑ์ว่าเสร็จ (acceptance)

- [ ] อัปโหลดรูป → ระบบรัน OCR อัตโนมัติ มี row ใน `silver_evidence_ocr`
- [ ] เคสหลักฐานตรงชัด → ระบบอนุมัติเอง + ชั่วโมงเข้าถูกหมวด (นับใน Gold/แดชบอร์ด)
- [ ] เคสไม่ชัด → ยังคง pending ให้เจ้าหน้าที่ตรวจ
- [ ] เจ้าหน้าที่เห็นข้อความ OCR + รูป และ override ได้
- [ ] pytest ผ่านทั้งหมด (mock OCR)

---

## หมายเหตุ / ข้อควรระวัง

- **ประสิทธิภาพ:** OCR บน CPU ช้า (หลายวินาทีต่อรูป) — ต้องทำเป็น background/async ไม่งั้น UI ค้าง
- **ความแม่นยำ:** อย่าโฆษณาว่า "แม่น 100%" ในเล่ม ให้ระบุว่าเป็น **ระบบช่วยคัดกรอง (assist)** ที่ auto-approve เฉพาะกรณีมั่นใจสูง ที่เหลือส่งให้คนตรวจ — ตรงกับความจริงและป้องกันคำถามยาก
- **เทสต์ต้อง mock OCR** อย่าเรียกโมเดลจริงในเทสต์ (ช้า + ไม่แน่นอน) ให้ inject ฟังก์ชัน `run_ocr` ปลอมที่คืนข้อความกำหนดเอง
- Silver นี้เข้ากับสถาปัตยกรรม Medallion ครบ: **Bronze (ไฟล์ดิบ) → Silver (OCR + validation) → Gold (นับเข้าแดชบอร์ด)** ตอบกรรมการได้เต็มวงจร
