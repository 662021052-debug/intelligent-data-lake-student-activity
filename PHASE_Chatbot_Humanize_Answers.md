# สเปก: ทำให้แชตบอตตอบเป็นภาษาคน (เลิกพ่นผลลัพธ์ดิบ key:value)

> "อ่าน PHASE_Chatbot_Humanize_Answers.md แล้วแก้ + เขียนเทสต์ เสร็จหยุดให้ฉันตรวจ"
> บังคับกฎที่ backend เสมอ · commit + push หลังตรวจ

## ปัญหา
ถามแชตบอต "กิจกรรมเต็มแล้วลงอันไหนดี" → ตอบเป็นลิสต์ดิบ:
`name: ..., category_name: ..., hours: 4.0, available_seats: 98` ต่อกันยาว ๆ
เพราะ intent สำเร็จรูปเอาแถวจากฐานข้อมูลมาต่อเป็นข้อความตรง ๆ ไม่ได้เรียบเรียง (path นี้ตั้งใจไม่เรียก LLM เพื่อความเร็ว — คงไว้)

## เป้าหมาย
ปรับ **ชั้นจัดรูปแบบคำตอบ** ของ intent สำเร็จรูปให้อ่านเป็นภาษาไทยธรรมชาติ อ่านง่าย โดย**ยังเป็น deterministic ไม่พึ่ง LLM** (ทำงานได้แม้ `LLM_BACKEND=stub`)

## ก่อนเริ่ม
- อ่าน `backend/app/chatbot_sql.py` — ดูว่า intent มีกี่ตัว, SQL แต่ละตัวคืนคอลัมน์อะไร, และจุดที่แปลงแถว→ข้อความอยู่ตรงไหน
- อ่าน `routers/chatbot.py` เพื่อดูรูปแบบ response ที่ frontend คาดหวัง (อย่าเปลี่ยน schema จนหน้าแชตพัง)

## สิ่งที่ต้องแก้
**1. เขียน formatter ภาษาไทยต่อ intent** แทนการ join key:value ดิบ ตัวอย่างแนวทาง:

- intent "กิจกรรมที่เปิดรับ / ที่นั่งว่าง":
  ```
  ตอนนี้มี {N} กิจกรรมที่ยังเปิดรับสมัคร (เรียงตามที่นั่งว่างมากสุด):
  1. {ชื่อ} — {หมวด} · {hours} ชม. · เหลือ {seats} ที่
  2. ...
  ```
  - เรียงตาม available_seats มาก→น้อย, ตัดกิจกรรมที่ seats=0 ออก (เพราะผู้ใช้ถามว่า "เต็มแล้วลงอันไหน")
  - ถ้าไม่มีเลย → "ตอนนี้ไม่มีกิจกรรมที่เปิดรับเพิ่มแล้ว"
- intent "ชั่วโมงของฉัน": สรุปเป็นประโยค เช่น "คุณสะสมได้ {รวม} ชม. — {หมวด A} {x} ชม., {หมวด B} {y} ชม. (ยังขาด {หมวด} อีก {z} ชม.)"
- intent "ขาดหมวดไหน": "คุณยังขาดหมวด {ชื่อหมวด} ({ที่ได้}/{เกณฑ์} ชม.) แนะนำให้เข้าร่วมกิจกรรมในหมวดนี้"
- intent อื่น ๆ ที่มี: ปรับให้เป็นประโยค/รายการมีหัวข้อ ไม่ใช่ key:value

**2. หลักการจัดรูปแบบร่วม**
- ไม่โชว์ชื่อคอลัมน์ดิบ (name/category_name/available_seats) ให้ผู้ใช้เห็น
- ตัวเลขชั่วโมงลงท้าย "ชม.", ที่นั่ง "ที่"
- ถ้ารายการยาวเกิน ~10 รายการ ให้ตัดแสดงบางส่วน + บอก "และอีก {k} กิจกรรม พิมพ์ถามเจาะจงได้"
- คงความเร็ว: ไม่เรียก LLM ใน path นี้

**3. (ออปชัน) คำถามแนว "แนะนำ"** เช่น "ลงอันไหนดี" — อย่างน้อยให้เรียงตามที่นั่งว่าง/หมวดที่ผู้ใช้ยังขาด แล้วขึ้นต้นว่า "แนะนำ:" ถ้าทำง่าย ถ้าซับซ้อนข้ามได้

## กฎที่ต้องคง
- ไม่เปลี่ยน response schema ที่ frontend ใช้ (แก้แค่เนื้อข้อความใน field เดิม)
- ทุก intent ที่มีอยู่ต้องยังตอบได้ ไม่ regress
- ยังเป็น deterministic — เทสต์ต้องคาดผลได้แน่นอน

## เทสต์
- แต่ละ intent: ยืนยันข้อความใหม่มีรูปแบบภาษาคน (มีคำนำ, ไม่มี "available_seats:" ดิบ)
- intent เปิดรับ: เรียงตามที่นั่งว่างถูกต้อง + ตัด seats=0 + เคสไม่มีกิจกรรม
- ไม่มีเทสต์เดิมพัง

## เกณฑ์ว่าเสร็จ
- [ ] ถาม "กิจกรรมเต็มแล้วลงอันไหนดี" → ได้คำตอบเป็นประโยค/รายการมีหัวข้อ ไม่ใช่ key:value ดิบ
- [ ] ทุก intent สำเร็จรูปอ่านเป็นภาษาไทยธรรมชาติ
- [ ] ยังไม่เรียก LLM (เร็วเท่าเดิม, ทำงานกับ stub ได้)
- [ ] pytest ผ่านครบ
