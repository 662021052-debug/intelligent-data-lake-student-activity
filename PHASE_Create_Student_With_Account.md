# สเปก: สร้างบัญชีนิสิตใหม่ในขั้นตอนเดียว (admin กรอกข้อมูลครบ + สร้างล็อกอินให้เลย)

> วิธีใช้: "อ่าน PHASE_Create_Student_With_Account.md แล้วทำ Backend ก่อน หยุดให้ฉันตรวจก่อนไป Frontend"
> บังคับกฎที่ backend เสมอ · เขียนเทสต์คุมทุกกฎ · commit + push

## เป้าหมาย
เดิมสร้างบัญชีนิสิตต้อง 2 ขั้น: สร้างระเบียน `student` ก่อน แล้วค่อยสร้าง `user` ผูก `student_id`
เปลี่ยนเป็น **ฟอร์มเดียว** — admin กรอกข้อมูลนิสิตครบ ระบบสร้าง `student` + `user (role=student)` พร้อมกันใน transaction เดียว โดยไม่ต้องมีนิสิตอยู่ก่อนและไม่ต้องผูกกับบัญชีเดิม

## ก่อนเริ่ม (สำคัญ)
- อ่าน `backend/app/models.py` เอา **ฟิลด์จริงทั้งหมดของ Student model** มาใช้ในฟอร์ม/สคีมา — อย่าเดาชื่อฟิลด์
- อ่าน endpoint สร้าง student / user เดิมใน `routers/students.py` และ `routers/*` เพื่อไม่ทำซ้ำและใช้ helper เดิม (`hash_password` ฯลฯ)

## Backend
**B1. Endpoint รวม** — `POST /students/with-account` (หรือเพิ่มโหมดใน `POST /students`)
- สิทธิ์: **admin เท่านั้น** (`require_admin`)
- body: ฟิลด์ทั้งหมดของ Student + `username` (ออปชัน) + `password` (ออปชัน) · ใช้ `extra="forbid"`
- ตรรกะใน **transaction เดียว** (ล้ม → rollback ทั้งคู่ ไม่เหลือ student ค้างที่ไม่มี user):
  1. ตรวจ **รหัสนิสิตซ้ำ** ในตาราง student → ซ้ำ → 400 "รหัสนิสิตนี้มีอยู่แล้ว"
  2. `username` default = รหัสนิสิต ถ้าไม่ส่งมา · ตรวจ **username ซ้ำ** ในตาราง user → ซ้ำ → 400
  3. `password` default = **รหัสนิสิต** ถ้าไม่ส่งมา (ตรงกับ convention seed) → hash ด้วย `hash_password`
  4. สร้าง `student` → สร้าง `user (role=student, student_id=ตัวใหม่, must_change_password=True ถ้ามีฟิลด์นี้)` → commit
- ตอบกลับ: ข้อมูลนิสิต + username ที่สร้าง (อย่าคืน password/hash)
- **เทสต์:** สร้างสำเร็จ (มีทั้ง student+user, ล็อกอินด้วย default ได้), รหัสนิสิตซ้ำ→400, username ซ้ำ→400, non-admin→403, ยัดฟิลด์ต้องห้าม→422, **atomic**: จงใจให้ user สร้างล้ม แล้วยืนยันว่าไม่มี student ตกค้าง

## Frontend (Flutter)
**F1. ฟอร์ม "เพิ่มนิสิตใหม่"** ในหน้าจัดการนิสิต (admin)
- ช่องกรอกครบทุกฟิลด์ของ Student (ตาม model จริง) + ช่อง username/password ที่ **เว้นว่างได้** (แสดง hint ว่า "เว้นว่าง = ใช้รหัสนิสิตเป็น username/รหัสผ่านเริ่มต้น")
- validate ฝั่ง client เบื้องต้น (จำเป็น/รูปแบบรหัสนิสิต) แล้วเรียก endpoint ใหม่
- สำเร็จ → แจ้ง "สร้างบัญชีนิสิตแล้ว · username: <...> · รหัสผ่านเริ่มต้น: รหัสนิสิต (แนะนำให้เปลี่ยน)" + รีเฟรชรายการ
- error จาก backend → แปลเป็นข้อความไทย (รหัสนิสิต/username ซ้ำ)

## กฎที่ต้องคง
- ไม่แตะ flow เดิมที่ผูก user กับ student ที่มีอยู่ (ยังต้องใช้ได้) — อันนี้เป็น "ทางลัดสร้างใหม่ในขั้นตอนเดียว" เพิ่มเข้ามา
- นิสิตที่สร้างใหม่เห็นเฉพาะข้อมูลตัวเอง (row-level เดิม)

## เกณฑ์ว่าเสร็จ
- [ ] admin กรอกฟอร์มเดียว → ได้ทั้งระเบียนนิสิตและบัญชีล็อกอิน
- [ ] นิสิตใหม่ล็อกอินด้วย username/password เริ่มต้น (รหัสนิสิต) ได้ทันที
- [ ] รหัสนิสิต/username ซ้ำ, non-admin, ฟิลด์ต้องห้าม → ถูกปฏิเสธด้วยข้อความไทย
- [ ] atomic (ยืนยันด้วยเทสต์ว่าล้มแล้วไม่เหลือ student ค้าง)
- [ ] flow ผูกกับบัญชีเดิมยังทำงานได้
- [ ] pytest + flutter test ผ่าน
