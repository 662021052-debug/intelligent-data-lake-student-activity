# สเปก F4: สแกน QR ด้วยกล้องปกติแล้วเด้งเข้าเว็บเช็กอิน (Deep-link check-in)

> ต่อยอดจาก Backend + F1 + F2 ที่เสร็จแล้ว
> วิธีใช้: "อ่าน PHASE_F4_DeepLink_Checkin.md แล้วทำ Backend ก่อน หยุดให้ฉันตรวจก่อนไป Frontend"
> บังคับกฎที่ backend เสมอ · เขียนเทสต์คุมทุกกฎ · commit + push

---

## เป้าหมาย

เดิม: นิสิตต้องเปิดแอป → ล็อกอิน → เข้าหน้าสแกน → สแกนในแอป
ใหม่: **นิสิตยกกล้องมือถือปกติส่อง QR → เด้งเปิดเบราว์เซอร์เข้าเว็บเช็กอินได้เลย**

เงื่อนไขที่ทำให้เป็นไปได้: QR ต้องเก็บ **URL เต็ม** ไม่ใช่ข้อความ token เปล่า ๆ (กล้อง iOS/Android ถึงจะขึ้นปุ่ม "เปิดลิงก์")

## การตัดสินใจ (ล็อกไว้แล้ว)
- **มีหน้ายืนยันก่อนเช็กอิน** — เด้งเข้าเว็บ → การ์ด "เช็กอิน: <ชื่อกิจกรรม>?" → กดยืนยัน (กันเช็กอินพลาด + เห็นว่าเช็กอินถูกกิจกรรม)
- **เก็บ in-app scanner (F1) ไว้เป็น fallback** — กล้องปกติเป็นทางหลัก, สแกนในแอป + กรอกรหัสมือยังอยู่

---

## Backend

**B3. เปลี่ยน QR payload เป็น URL**
- เพิ่ม config `PUBLIC_APP_BASE_URL` (เช่น `https://activity.tsu.ac.th`) — โดเมนจริงตอน deploy
- `qr_payload()` → ออกเป็น `{PUBLIC_APP_BASE_URL}/#/checkin?c=<token>`
  - หมายเหตุ: ใช้ `/#/` เพราะ Flutter web ใช้ hash routing เป็นค่าเริ่มต้น (ถ้าโปรเจกต์ตั้ง path-url-strategy แล้วให้ตัด `#` ออก — Claude Code เช็กจาก main.dart)
- `normalize_token()` → รับ **3 รูปแบบ**: URL เต็ม (ดึง `c=` query), แบบ prefix `TSU-CHECKIN:<token>` เดิม, และ token เปล่า (กรอกมือ) — คงความเข้ากันได้ย้อนหลัง
- endpoint `/activities/{id}/checkin-qr` คืน `qr_payload` (URL) ตามเดิม เพิ่มคืน `token` ล้วนไว้ให้กรอกมือ
- **เทสต์:** normalize จาก URL ได้ token ถูก, จาก prefix ได้, จาก token เปล่าได้, URL ผิดรูป/ไม่มี `c=` → 400 "QR ไม่ถูกต้อง"; endpoint checkin เดิมทำงานเหมือนเดิมทุกกฎ (D2, ช่วงเวลา, ซ้ำ, walk-up)

## Frontend

**F4a. Route `/checkin?c=<token>` (deep link)**
- เพิ่ม route รับ token จาก URL ตอนเปิดหน้า
- **ล็อกอินเป็นนิสิตอยู่แล้ว** → เรียก `GET /activities/{id}/...`/หรือ decode ชื่อกิจกรรมเพื่อโชว์ → การ์ดยืนยัน "เช็กอิน: <ชื่อกิจกรรม>?" → ปุ่ม "ยืนยันเช็กอิน" → `POST /participations/checkin` → การ์ดเขียวสำเร็จ (เหมือน F1) + เตือน D2 (ยังต้องส่งหลักฐาน)
  - หมายเหตุ: ถ้าไม่อยากยิง API เพิ่มเพื่อเอาชื่อ ให้ยืนยันด้วยข้อความกลาง ๆ "ยืนยันเช็กอินกิจกรรมนี้?" ก็ได้ — ผลลัพธ์ตอบชื่อกิจกรรมกลับมาหลังเช็กอินอยู่แล้ว
- **ยังไม่ล็อกอิน** → เด้งไปหน้า login โดย**จำ token ไว้** (query/redirect param) → ล็อกอินเสร็จ → กลับมาหน้ายืนยันเช็กอินอัตโนมัติ ไม่ให้ token หาย
- **ล็อกอินเป็น staff/admin หรือบัญชีไม่ผูกนิสิต** → ข้อความ "บัญชีนี้เช็กอินไม่ได้ (เฉพาะนิสิต)" — ไม่ยิง checkin (backend ตอบ 403 อยู่แล้ว แต่กันไว้ที่ UI ให้ข้อความชัด)

**F4b. แก้ login flow ให้พา redirect ต่อ**
- หน้า login รับ param ปลายทาง (เช่น `?redirect=/checkin?c=...`) → หลังล็อกอินสำเร็จพาไปปลายทางนั้นแทนหน้า home
- ถ้าไม่มี param → ไปหน้า home ตามเดิม

**F4c. คงของเดิม**
- in-app scanner (F1) + กรอกรหัสมือ + หน้าแสดง QR ฝั่งเจ้าหน้าที่ (F2) ไม่ต้องแก้ตรรกะ — แค่ QR ที่ F2 แสดงตอนนี้เป็น URL แล้ว สแกนได้ทั้งกล้องปกติและ in-app

---

## ข้อจำกัด / ต้องรู้
- **ต้องมี HTTPS + โดเมนจริง** ตอน deploy — URL ใน QR ชี้ localhost ไม่ได้ (มือถือเครื่องอื่นเปิดไม่ถึง) ตอน dev ทดสอบ deep link ได้ในเครื่องเดียวผ่าน localhost
- **D1 เหมือนเดิม** — ลิงก์ที่หลุด/ถ่ายรูปส่งต่อยังใช้ได้แค่ในช่วงเวลาเช็กอิน และเช็กอินไม่ให้ชั่วโมง → แรงจูงใจโกงต่ำ ไม่ต้องทำ QR หมุน
- **D2 เหมือนเดิม** — เช็กอิน = บันทึกการมาเท่านั้น ชั่วโมงยังต้องส่งหลักฐาน + อนุมัติ

## เกณฑ์ว่าเสร็จ
- [ ] QR (จาก F2) สแกนด้วยกล้องมือถือปกติ → ขึ้นปุ่มเปิดลิงก์ → เข้าเว็บหน้ายืนยันเช็กอิน
- [ ] ยังไม่ล็อกอิน → login แล้วกลับมาเช็กอินต่อ token ไม่หาย
- [ ] ล็อกอินอยู่แล้ว → ยืนยัน → เช็กอินสำเร็จ hours_earned ยัง 0
- [ ] staff/บัญชีไม่ผูกนิสิต → ข้อความปฏิเสธชัดเจน
- [ ] in-app scanner + กรอกมือเดิมยังใช้ได้ (normalize รับทั้ง URL/prefix/token เปล่า)
- [ ] เทสต์ backend + flutter ผ่านครบ
