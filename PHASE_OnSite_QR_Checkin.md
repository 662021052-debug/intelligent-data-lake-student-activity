# สเปก: ลงทะเบียนหน้างานด้วย QR (นิสิตสแกนเช็กอินเอง) — ขอบเขต 6.4

> วิธีใช้: วางไฟล์นี้ในโฟลเดอร์โปรเจกต์ แล้วสั่ง Claude Code:
> "อ่าน PHASE_OnSite_QR_Checkin.md แล้วทำทีละส่วน เริ่ม Backend ก่อน หยุดให้ฉันตรวจก่อนไป Frontend"
>
> บังคับกฎที่ backend เสมอ · เขียน pytest คุมทุกกฎ · commit + push

---

## แนวคิด

แก้ปัญหาหลักตามข้อเสนอ (ข้อมูลเช็กอินหน้างานตกหล่น) — **แต่ละกิจกรรมมี QR ประจำกิจกรรม** เจ้าหน้าที่แสดงที่หน้างาน นิสิตเปิดแอปสแกน QR → ระบบบันทึก `check_in_time` ทันที เรียลไทม์ ไม่ผ่านมือคน และผูกกับ **นิสิตที่ล็อกอินอยู่** (สแกนแทนกันไม่ได้)

**หลักการสำคัญ (ตัดสินใจแล้ว):**
- สแกน QR = บันทึก **"มาร่วมงาน" (check_in_time) เท่านั้น** — ไม่ให้ชั่วโมงทันที
- **ชั่วโมงยังต้องส่งหลักฐาน + เจ้าหน้าที่อนุมัติ** ตามกฎเดิมทุกอย่าง (ไม่แตะกฎ A3)
- แยกหน้าที่ชัดเจน: **check-in บอก "ใครมา"** (ข้อมูลเรียลไทม์ที่เดิมตกหล่น) · **หลักฐาน+อนุมัติบอก "ใครได้ชั่วโมง"**

---

## Backend

**B1. เพิ่ม token ประจำกิจกรรม**
- เพิ่มฟิลด์ `checkin_token` (uuid, unique) ในตาราง activity — generate ตอนสร้างกิจกรรม (Alembic migration + backfill ของเดิม)
- `GET /activities/{id}/checkin-qr` (staff เจ้าของ / admin) → คืน token/payload สำหรับให้ frontend สร้าง QR แสดงที่หน้างาน

**B2. `POST /participations/checkin`** — นิสิตสแกนแล้วยิงมา
- body: `{ token }` · auth = **student ที่ผูกบัญชีแล้วเท่านั้น** (student_id มาจาก JWT — เช็กอินให้คนอื่นไม่ได้)
- ตรรกะ (ตามลำดับ ปฏิเสธเป็นข้อความไทย):
  1. หา activity จาก token → ไม่พบ → 400 "QR ไม่ถูกต้อง"
  2. activity ต้อง `approved` → ไม่ใช่ → 400
  3. **เช็กช่วงเวลา** — `now` ต้องอยู่ในช่วง `start_at ± หน้าต่างที่กำหนด` (เช่น เปิดเช็กอินก่อน 1 ชม. ถึงหลังจบ) → นอกช่วง → 400 "ยังไม่ถึงเวลาเช็กอิน / เลยเวลาแล้ว"
  4. หา participation ของนิสิตคนนี้กับกิจกรรมนี้:
     - ถ้ามีอยู่แล้ว (สมัครล่วงหน้า) → ตั้ง `check_in_time = now`
     - ถ้ายังไม่มี (walk-up มาหน้างานเลย) → สร้าง participation ใหม่ (ถ้ายังไม่เต็ม) แล้วตั้ง check_in_time; เต็ม → 400 "กิจกรรมเต็มแล้ว"
  5. เช็กอินไปแล้ว → 400 "คุณเช็กอินกิจกรรมนี้แล้ว"
- **pytest:** เช็กอินถูกต้อง→200, token ผิด→400, นอกช่วงเวลา→400, เช็กอินซ้ำ→400, walk-up ตอนเต็ม→400, staff/admin เรียก→403, นิสิตอื่นเช็กอินแทน (ยิง token เดียวกัน) ได้เฉพาะของตัวเอง

## Frontend (Flutter Web)

**F1. ฝั่งนิสิต — สแกน QR**
- เพิ่มการ์ด "เช็กอินหน้างาน" ในหน้า Home ของนิสิต
- ใช้ package กล้องสแกน QR (`mobile_scanner`) — เปิดกล้อง → สแกน QR ของกิจกรรม → ดึง token → `POST /participations/checkin`
- สำเร็จ → แสดงชื่อกิจกรรม + เวลาเช็กอิน + ✓; ล้มเหลว → ข้อความไทยชัดเจน

**F2. ฝั่งเจ้าหน้าที่ — แสดง QR**
- ในหน้ากิจกรรมของตน มีปุ่ม "แสดง QR เช็กอิน" → เปิด QR ขนาดใหญ่เต็มจอ (สร้างจาก token ด้วย `qr_flutter`) ไว้ฉายที่หน้างาน

**F3. หมายเหตุทางเทคนิค (สำคัญ)**
- สแกนกล้องบน Flutter Web ต้องเป็น **HTTPS** (หรือ localhost ตอน dev) และผู้ใช้ต้อง**อนุญาตกล้อง** — จัด flow ขออนุญาต + fallback ถ้ากล้องใช้ไม่ได้ (เช่น กรอกรหัสกิจกรรมมือแทน)
- บนเซิร์ฟเวอร์มหาลัยต้องมี HTTPS ไม่งั้นกล้องเปิดไม่ได้

---

## กฎธุรกิจ (ตัดสินใจแล้ว)

**D1. กันสแกน QR จากรูปถ่าย — ความสำคัญต่ำ**
- เพราะเช็กอินไม่ให้ชั่วโมง ต่อให้แอบสแกนก็ไม่ได้อะไร (ยังต้องส่งหลักฐาน+อนุมัติ) → แรงจูงใจโกงต่ำ
- ทำแค่ **จำกัดช่วงเวลาเช็กอิน** (start_at ± หน้าต่าง) พอ — ไม่ต้องทำ QR หมุน

**D2. เช็กอิน ≠ ชั่วโมง — ยืนยันแล้ว**
- สแกน QR แค่บันทึก `check_in_time` (มาร่วมงาน) · ชั่วโมงยังต้อง**ส่งหลักฐาน + เจ้าหน้าที่อนุมัติ**ตามกฎ A3 เดิม
- **ไม่แตะกฎการอนุมัติ/นับชั่วโมงเดิมเลย**

---

## เกณฑ์ว่าเสร็จ
- [ ] นิสิตสแกน QR ของกิจกรรม → check_in_time ถูกบันทึก เห็นผลทันที
- [ ] เจ้าหน้าที่แสดง QR ของกิจกรรมตัวเองได้
- [ ] เช็กอินนอกช่วงเวลา / ซ้ำ / token ผิด → ถูกปฏิเสธด้วยข้อความไทย
- [ ] walk-up (ไม่ได้สมัครล่วงหน้า) เช็กอินแล้วได้ participation ใหม่
- [ ] pytest ผ่านครบ
