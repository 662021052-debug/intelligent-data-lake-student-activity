# สเปก: ตัดการผูกนิสิตตอนสร้างบัญชี — สร้างนิสิตใหม่ต้องกรอกข้อมูลครบเสมอ

> "อ่าน PHASE_Remove_Student_Linking_At_Create.md แล้วทำ Backend ก่อน หยุดให้ฉันตรวจก่อนไป Frontend"
> บังคับกฎที่ backend เสมอ · เขียนเทสต์คุมทุกกฎ · commit + push

## ปัญหาที่แก้
dialog "เพิ่มผู้ใช้" ปัจจุบันเลือก role = นิสิต + "ยังไม่ผูกข้อมูลนิสิต" ได้ → เกิดบัญชีนิสิต `student_id = null`
ที่ใช้ระบบไม่ได้ (แดชบอร์ดชั่วโมง/สมัคร/หลักฐาน/row-level ล้วนอ้าง student_id)

## หลักการใหม่ (ตัดสินใจแล้ว)
- **สร้างนิสิตใหม่** = กรอกข้อมูลนิสิตให้ครบในฟอร์มเดียว → สร้าง `student` + `user(role=student)` พร้อมกัน (ใช้ endpoint เดิม `POST /students` โหมด create account)
- **สร้าง staff/admin** = username/password/role เท่านั้น
- **ตัดตัวเลือก "ผูกกับนิสิตที่มีอยู่" ออกจากการสร้างบัญชีทั้งหมด**
- หมายเหตุ: seed สร้างบัญชีให้นิสิตทุกคนอยู่แล้ว (username = รหัสนิสิต) การตัด flow ผูกจึงไม่ทำให้ใครล็อกอินไม่ได้

## Backend
**B1. บังคับ invariant: user role=student ต้องมี student_id เสมอ**
- generic endpoint สร้าง user (`POST /users` หรือที่ dialog นี้ยิง): **อนุญาต role ∈ {staff, admin} เท่านั้น**
  - ส่ง role=student มา → 400 "การสร้างบัญชีนิสิตต้องใช้ฟอร์มเพิ่มนิสิต (กรอกข้อมูลนิสิตให้ครบ)"
- ทางเดียวที่สร้าง user role=student ได้คือ `POST /students` (สร้าง student+account พร้อมกัน) — คงไว้
- ถ้ามีที่ไหนยัง set student_id ให้ user ได้แบบแยก/null ให้ปิด
- **เทสต์:** `POST /users` role=student → 400 · role=staff/admin → สร้างได้ · `POST /students` ยังสร้าง student+account ได้ · ไม่มีทางได้ user role=student ที่ student_id เป็น null

## Frontend (Flutter)
**F1. แก้ dialog "เพิ่มผู้ใช้" (หน้าจัดการผู้ใช้)**
- role dropdown เหลือ **staff / admin** เท่านั้น (เอา "นิสิต" ออก)
- **ลบ dropdown "นิสิตที่ผูกบัญชี" ทิ้งทั้งหมด**
- เหลือแค่ ชื่อผู้ใช้ / รหัสผ่าน / สิทธิ์(staff,admin)
- เพิ่มข้อความ/ปุ่มบอกทาง: "ต้องการเพิ่มนิสิต? ไปที่หน้าจัดการนิสิต → เพิ่มนิสิต"

**F2. ยืนยันว่าฟอร์ม "เพิ่มนิสิต" (ที่ทำไว้แล้ว) เป็นทางเดียวของการสร้างนิสิต** — ไม่ต้องแก้ตรรกะ แค่ให้แน่ใจว่าลิงก์/ปุ่มเข้าถึงง่าย

## (ออปชัน — ถ้าจะทำควบไปเลย) แก้ DELETE นิสิตที่มีประวัติ
> ถ้าไม่เอาส่วนนี้ ลบทิ้งได้ — แต่แนะนำทำเพราะเกี่ยวเนื่องกัน
- นิสิต **ไม่มี participation** → hard delete ได้ (ลบ student + user ที่ผูก) เหมือนที่ทำแล้ว
- นิสิต **มี participation/หลักฐาน** → **บล็อก** ตอบ 400 "ลบไม่ได้: นิสิตมีประวัติเข้าร่วม N รายการ" (กัน lineage หาย)
- เทสต์: ลบนิสิตมีประวัติ → 400 + ข้อมูลไม่หาย · ลบนิสิตไม่มีประวัติ → ลบทั้ง student และ user

## เกณฑ์ว่าเสร็จ
- [ ] dialog "เพิ่มผู้ใช้" สร้างได้แค่ staff/admin ไม่มีตัวเลือกนิสิต/ผูกนิสิต
- [ ] `POST /users` role=student → 400 พร้อมข้อความบอกทางไปฟอร์มเพิ่มนิสิต
- [ ] สร้างนิสิตใหม่ผ่านฟอร์มเพิ่มนิสิต (ข้อมูลครบ + บัญชี) ได้ปกติ
- [ ] ไม่มีทางเกิด user role=student ที่ student_id = null
- [ ] (ถ้าทำ DELETE) บล็อกลบนิสิตที่มีประวัติ
- [ ] pytest + flutter test ผ่าน
