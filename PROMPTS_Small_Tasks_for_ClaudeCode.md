# Prompts สำหรับ Claude Code — งานเล็กที่ค้าง (จาก PDF ฟีดแบ็ก)

ก็อปทีละบล็อก ทำทีละอัน ทุกอันสั่งให้สำรวจโค้ดเดิมก่อนและหยุดให้ตรวจก่อน commit

---

## Prompt A — แก้ชื่อกิจกรรมได้ (เผื่อเปลี่ยนชื่อในปีถัดไป)

```
สำรวจ endpoint PUT /activities/{id} และฟอร์มแก้ไขกิจกรรมฝั่ง Flutter ก่อน 
แล้วทำให้ "ชื่อกิจกรรม (name)" แก้ไขได้:

- ตรวจว่า field name อยู่ใน ActivityUpdate DTO และ PUT รับค่าใหม่ได้จริง 
  (ถ้าถูกล็อกไว้ ให้ปลดล็อก)
- ฟอร์มแก้ไขฝั่ง Flutter: ช่องชื่อกิจกรรมต้องแก้ได้ ไม่ใช่ read-only
- กติกาเดิมคงไว้: แก้แล้วกิจกรรมกลับเป็นสถานะ pending รออนุมัติ
- ไม่กระทบ checkin_token หรือความสัมพันธ์อื่น

เขียน/อัปเดต test ว่าแก้ชื่อแล้วบันทึกได้และสถานะกลับเป็น pending 
ทำเสร็จหยุดให้ฉันตรวจก่อน commit
```

---

## Prompt B — หน้านิสิตแสดงกิจกรรมทันทีตอนเข้า

```
สำรวจหน้าแรกฝั่งนิสิต (Flutter) และ endpoint ดูรายการกิจกรรมก่อน แล้วปรับให้
นิสิตเห็นรายการกิจกรรมทันทีที่เข้าหน้า โดยไม่ต้องกดค้นหา/กรองก่อน:

- โหลดกิจกรรมที่ approved + ยังไม่ผ่านวันจัด แสดงเป็นค่าเริ่มต้นทันที
- เรียงตามวันเวลาที่ใกล้ที่สุดก่อน
- ยังคงมีช่องค้นหา/กรองไว้ให้ใช้ต่อได้ แค่ไม่ต้องกดก่อนถึงจะเห็น
- ตรวจว่ามี state ว่างเปล่า (empty state) ข้อความไทยเมื่อไม่มีกิจกรรม
- เช็กว่ามีข้อมูลอะไรที่ควรโชว์เพิ่มไหม (วันเวลา, สถานที่, ชั่วโมง, จำนวนที่รับ/เหลือ) 
  ให้การ์ดกิจกรรมมีข้อมูลพอให้นิสิตตัดสินใจสมัครได้

ทำเสร็จหยุดให้ฉันตรวจก่อน commit
```

---

## Prompt C — ลบกิจกรรมไม่ได้ให้ซ่อนแทน (soft delete)

```
สำรวจ DELETE /activities/{id} และ model Activity ก่อน แล้วเปลี่ยนพฤติกรรมการลบ:

- ถ้ากิจกรรมมีผู้เข้าร่วม (participation) ผูกอยู่ → ห้ามลบจริง ให้ "ซ่อน" แทน
- เพิ่มคอลัมน์ is_hidden (bool, default false) + Alembic migration 
  (pattern เพิ่ม nullable → backfill → NOT NULL เหมือน migration ก่อนๆ)
- DELETE: ถ้าไม่มี participation ผูก → ลบจริงได้; ถ้ามี → set is_hidden=true แทน
- กิจกรรมที่ is_hidden=true: ไม่แสดงในรายการนิสิต และไม่ให้สมัครใหม่ 
  แต่ยังเห็นในฝั่ง staff/admin (มี badge "ซ่อนอยู่") และข้อมูลการเข้าร่วมเดิมไม่หาย
- เพิ่มปุ่ม "เลิกซ่อน" ให้ staff/admin กู้คืนได้
- gold views / การนับชั่วโมงเดิมของคนที่เข้าร่วมไปแล้วต้องไม่กระทบ

เขียน test: ลบกิจกรรมมีผู้เข้าร่วม → ถูกซ่อนไม่ใช่ลบ, นิสิตไม่เห็น, ชั่วโมงเดิมยังอยู่
ทำเสร็จหยุดให้ฉันตรวจก่อน commit
```

---

## หมายเหตุ
- Prompt C แตะ schema (เพิ่มคอลัมน์ + migration) หนักสุด ทำเป็นอันสุดท้าย
- ก่อนทำ ลองถาม Claude Code ว่า "งานที่ 2 (admin จัดการหมวดชั่วโมง) ในไฟล์ FIX ก่อนหน้า ทำหรือยัง" เผื่อยังค้างจะได้เก็บด้วย
