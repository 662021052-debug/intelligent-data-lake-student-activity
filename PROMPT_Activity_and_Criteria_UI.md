# Prompt สำหรับ Claude Code — ปรับ UI: ฟอร์มสร้างกิจกรรม + หน้าเกณฑ์ ให้หายสับสน

**ภาพอ้างอิง:** `ui_mockup_activity_and_criteria.html` (โฟลเดอร์รายงาน)

**บริบท (จากผล audit):** `activity.activity_type` ไม่เกี่ยวกับการนับชั่วโมงเลย (หมวด A = 0) เป็นแค่ป้ายแสดง/กรอง/ค้นหา
ตัวจริงที่กำหนดชั่วโมงคือ `activity_requirement` → **ตัดสินใจ: เก็บประเภทไว้เป็นแท็ก แก้ที่ UI ให้ชัด**

**คงธีมเทา-ฟ้า + Sarabun + คอมโพเนนต์กลางเดิม · ไม่แตะ business logic / การนับชั่วโมง / backend schema**
ทำ 2 งาน หยุดให้ตรวจแต่ละงาน · ทุกงาน analyze / `flutter test` (ธรรมดา) / build ต้องผ่าน

---

## งานที่ 1 — ฟอร์มสร้าง/แก้กิจกรรม: แยก "requirement (พระเอก)" กับ "ประเภท (ป้ายรอง)"
ปัญหาเดิม: dropdown ประเภท กับ RequirementPicker วางปนกันจนดูเท่ากัน ผู้ใช้งงว่าอันไหนกำหนดชั่วโมง

- **บล็อก "นับเข้าเกณฑ์ชั่วโมง" (RequirementPicker) = เด่นสุด** วางเป็นการ์ดพื้นฟ้าอ่อนมีกรอบ มีหัวข้อ + คำอธิบาย
  "ตัวนี้คือสิ่งที่กำหนดว่านิสิตได้ชั่วโมงหมวดไหน · เลือกได้หลายข้อ" · โชว์รายการที่เลือกเป็น pill พร้อมชั่วโมง + ปุ่มลบ + ปุ่ม "＋ เพิ่มเกณฑ์ที่นับเข้า"
  (ใช้ RequirementPicker เดิม แค่จัดกรอบ/หัวข้อใหม่ ไม่แตะ logic การผูก)
- **ช่อง "ประเภทกิจกรรม" = ป้ายรอง** ย้ายลงไปล่าง ๆ · label ต่อท้ายว่า **"— ป้ายไว้ค้นหา/กรอง ไม่เกี่ยวกับการนับชั่วโมง"**
  - เปลี่ยนจาก dropdown เป็น chip เลือก (segmented) 5 ค่าเดิม (จิตอาสา/กีฬา/วิชาการ/ศิลปวัฒนธรรม/อบรม-สัมมนา) — คงค่าที่ backend รับเหมือนเดิม
- ลำดับฟิลด์ตาม mockup: ชื่อ → **บล็อกเกณฑ์** → ชั่วโมง/จำนวนรับ → วันเวลา/สถานที่ → ประเภท
- **ไม่เปลี่ยน payload ที่ส่ง backend** — ยังส่ง activity_type + activity_requirement เหมือนเดิม แค่จัดวาง UI ใหม่
- เก็บงานเสริมจาก audit: ให้ dropdown/chip ประเภทมี default ที่ชัด (ไม่ปล่อยให้ค่าแรกติดมาเงียบ ๆ) — ถ้าจะบังคับก็ใส่ validator, ถ้าให้ว่างได้ต้องรองรับ (ดูงานที่ราวๆ activities_screen.dart:1029)

หยุดให้ฉันตรวจ

---

## งานที่ 2 — หน้าเกณฑ์: โชว์ "ช่วงรุ่นที่คำนวณได้"
ปัญหาเดิม: ฟอร์มโชว์แค่ "ปีรุ่นที่เริ่มใช้" คนเลยสงสัยว่าเกณฑ์ใช้ถึงรุ่นไหน

- หัวการ์ดแต่ละชุดเพิ่ม **ป้ายช่วงรุ่นที่คำนวณจากชุดถัดไป** (ไม่ใช่ค่าที่กรอก):
  - "เกณฑ์ 2567" → ป้าย **"ใช้กับรหัส 67–69"** (เพราะยังไม่มีชุด 2570) ถ้าเพิ่มชุด 2570 ต้องกลายเป็น "67–69" อัตโนมัติ
  - "เกณฑ์เดิม" → **"ใช้กับรหัส ≤66"**
  - ชุดล่าสุด (ไม่มีชุดถัดไป) → **"รหัส 67 ขึ้นไป"**
  - สูตร: ช่วง = [effective_from_cohort ของชุดนี้ .. (effective_from_cohort ของชุดถัดไปที่มากกว่า − 1)] ต่อ program_type เดียวกัน
    → คำนวณฝั่ง frontend จากรายการชุดที่มีอยู่ (GET /criteria-sets เดิม) **ไม่ต้องเพิ่ม backend**
- คง "(ปัจจุบัน = ปี 1–3 / ปี 4)" เป็นวงเล็บช่วยเสริม แต่ให้ **รหัสนิสิตเป็นตัวหลัก** (คงที่ ไม่เลื่อนตามปี)
- ในฟอร์ม "เพิ่มชุดเกณฑ์ใหม่": ใต้ช่อง "ปีรุ่นที่เริ่มใช้" เพิ่มข้อความพรีวิว เช่น "ชุดนี้จะใช้กับรหัส 70 ขึ้นไป และทำให้ชุด 2567 เหลือ 67–69"
  (พรีวิวสด ๆ ตามเลขที่พิมพ์)
- เพิ่มบรรทัดช่วยจำท้ายหน้า (ตาม mockup): อธิบายว่าเพิ่มชุดใหม่แล้วชุดเก่าหดช่วงเองอัตโนมัติ ไม่ต้องแก้ชุดเก่า

หยุดให้ฉันตรวจ

## ข้อควรระวัง
- **ห้ามแตะการนับชั่วโมง / resolver / completion / gold** — งานนี้เป็น UI ล้วน
- ประเภท: ค่าที่ backend รับต้องเหมือนเดิม (5 ค่า string) อย่าเปลี่ยนชื่อค่า
- ช่วงรุ่นเป็นค่า derive ฝั่ง UI — ห้าม hardcode "67–69" ต้องคำนวณจากรายการชุดจริง (ทดสอบ: สมมติเพิ่มชุด 2570 แล้วป้าย 2567 ต้องเป็น 67–69 เอง)
- เทส: `flutter test` ธรรมดา · unit คุมการคำนวณช่วงรุ่น (2 ชุด / 3 ชุด / ชุดเดียว) + ฟอร์มกิจกรรมยังส่ง payload เดิม
- E2E: เปิดฟอร์มสร้างกิจกรรม เห็นบล็อกเกณฑ์เด่น + ประเภทเป็น chip รอง · เปิดหน้าเกณฑ์เห็นป้าย "67–69"/"≤66"

เริ่มงานที่ 1 ก่อน เสร็จหยุดให้ฉันตรวจ
