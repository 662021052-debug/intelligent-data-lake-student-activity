# Prompt สำหรับ Claude Code — AUDIT: "ประเภทกิจกรรม" (activity.type) ถูกใช้ทำอะไรบ้าง

**อ่านอย่างเดียว — ห้ามแก้/ลบโค้ดใด ๆ · ไม่ต้อง build/test · แค่รายงานกลับ**

บริบท: ตอนนี้การนับชั่วโมงใช้ `activity_requirement` (ผูกกิจกรรม→requirement ของ criteria_set) เป็นตัวจริงแล้ว
เราสงสัยว่า `activity.type` (ประเภท: จิตอาสา/วิชาการ/อบรม...) ยังจำเป็นไหม หรือกลายเป็นของซ้ำซ้อน
ต้องรู้ก่อนว่ามันถูกใช้ขับเคลื่อนอะไรจริง ๆ ก่อนจะตัดสินใจเก็บ/ตัด

## ให้ค้นและรายงาน (แยกหัวข้อ)

### 1. โมเดล/สคีมา
- `activity.type` (หรือชื่อฟิลด์จริง เช่น category/activity_type) ประกาศที่ไหน · เป็น enum หรือ free text · nullable ไหม · มี default ไหม
- มี FK/ตารางประเภทแยกไหม หรือเก็บเป็น string ในตาราง activity

### 2. Backend — จุดที่ "ใช้ค่า type ไปทำอะไร" (ไม่ใช่แค่อ่าน-ส่งออก)
ไล่หา `type` / `category` ใน `app/**` แล้วแยกว่าแต่ละจุดเป็น:
- **(A) ใช้ตัดสินตรรกะ** (if type == ..., group by type, คำนวณชั่วโมง/completion/at-risk จาก type) — จุดนี้สำคัญสุด
- **(B) แค่กรอง/ค้นหา** (filter query param by type)
- **(C) แค่แสดง/ส่งออก** (คืนใน response, ใส่ใน export/PDF/Excel, QR, รายงาน gold)
- โดยเฉพาะเช็ก: completion.py, gold.py, approval.py, criteria.py, routers/activities.py, export/report ใด ๆ — type ไปยุ่งกับการนับชั่วโมงหรือไม่ (คาดว่า "ไม่" แต่ขอยืนยัน)

### 3. Frontend — จุดที่ใช้ type
- ไดอะล็อกสร้าง/แก้กิจกรรม: ช่องเลือกประเภท bind กับอะไร · required ไหม
- ตาราง/การ์ด/ฟิลเตอร์กิจกรรมที่โชว์หรือกรองด้วยประเภท
- หน้านิสิต (สมัคร/ปฏิทิน) ใช้ประเภทแสดงผลไหม

### 4. ความสัมพันธ์ type ↔ requirement
- มีจุดไหนที่ type กับ activity_requirement **ต้องสอดคล้องกัน** หรือ derive กันไหม (เช่น เลือกประเภทแล้ว auto-suggest requirement)
- ค่า type ที่มีในระบบตอนนี้มีกี่แบบ (distinct) และแต่ละแบบมีกิจกรรมกี่อัน (query ข้อมูลจริง)

## รูปแบบรายงาน
ตอบเป็นตารางสรุปต่อจุดใช้งาน: `ไฟล์:บรรทัด | หมวด (A/B/C) | ทำอะไร` +
ปิดท้ายด้วยข้อสรุป 2–3 บรรทัด: "type ยุ่งกับการนับชั่วโมงไหม", "ถ้าตัด type ออกจะกระทบอะไรบ้าง (ฟีเจอร์/หน้าจอ/รายงาน)"

**ห้ามแก้อะไร — รายงานกลับมาให้ตรวจก่อนตัดสินใจ**
