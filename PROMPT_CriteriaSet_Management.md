# Prompt — จัดการชุดเกณฑ์: ดูทั้ง 2 ชุด + สร้างชุดใหม่สำหรับรุ่นอนาคต

เป้าหมาย: หน้า "หมวดชั่วโมงกิจกรรม" แสดงชุดเกณฑ์ทุกชุดแยกตามรุ่น และให้แอดมิน **สร้างชุดเกณฑ์ใหม่สำหรับรุ่นอนาคต**
โดยระบบแมปรุ่น→ชุดแบบยืดหยุ่น (ไม่ต้องแก้โค้ดเมื่อเพิ่มชุดใหม่) · ทำเป็นเฟส หยุดให้ตรวจทุกเฟส · ทุกเฟส test/build ผ่าน

## แนวคิดหลัก
- เกณฑ์ทางการที่ seed ไว้ (legacy, 2567-regular) = **ชุดระบบ อ่านอย่างเดียว** (กันแก้ของทางการพลาด + กัน seed_criteria.py เขียนทับ)
- เกณฑ์รุ่นอนาคต = **ชุด custom** ที่แอดมินสร้าง/แก้/ลบเองได้
- การแมป "นิสิตรหัส NN → ชุดไหน" เปลี่ยนจาก hardcode (≥2567) เป็น **data-driven** ด้วยฟิลด์ปีรุ่นที่เริ่มใช้

---

## เฟส 1 — schema + ตรรกะแมปรุ่น (extensible)
- เพิ่ม `criteria_set.effective_from_cohort` (INT — ปีรุ่นที่เริ่มใช้ เช่น 2567) และ `criteria_set.is_system` (BOOL)
- migration + backfill: legacy → effective_from_cohort ต่ำสุด (เช่น 0) is_system=true · 2567-regular → 2567 is_system=true
- **เขียน resolver ใหม่** (criteria.py `criteria_set_code_for`): เลือกชุดที่ program_type ตรง และ `effective_from_cohort` **สูงสุดที่ ≤ cohort ของนิสิต** ; ไม่มี → legacy
  → เพิ่มชุดรุ่น 2570 ทีหลัง นิสิตรหัส 70↑ จะแมปเข้าชุดใหม่เองอัตโนมัติ ไม่ต้องแก้โค้ด
- `seed_criteria.py`: enforce เฉพาะชุด `is_system=true` เท่านั้น · **ห้ามแตะชุด custom** (is_system=false) ตอน boot
- เทส: resolver เลือกชุดถูกตามปีรุ่น (66→legacy, 68→2567, สมมติ 70→ชุดใหม่ถ้ามี), seed ไม่ทับ custom

หยุดให้ตรวจ

## เฟส 2 — CRUD API (admin เท่านั้น)
- `POST/PUT/DELETE /criteria-sets` — สร้าง/แก้/ลบชุด (code, name, academic_year, program_type, effective_from_cohort, total_required_hours, counting_rule)
- CRUD `talent`, `requirement`, `requirement_group` ภายในชุด
- **safety:**
  - ชุด `is_system=true` → แก้/ลบไม่ได้ (409/403 พร้อมข้อความ) อ่านอย่างเดียว
  - ลบชุดที่มีนิสิต assign อยู่ไม่ได้ (400 บอกจำนวน)
  - ตรวจ effective_from_cohort ห้ามซ้ำใน program_type เดียวกัน
  - validate ผลรวมชั่วโมง (เตือนถ้าไม่เท่า total_required_hours)
- เทส: สร้างชุด custom ได้, แก้/ลบ system ไม่ได้, ลบชุดที่มีนิสิตไม่ได้, resolver ใช้ชุด custom กับรุ่นที่ตรง

หยุดให้ตรวจ

## เฟส 3 — frontend หน้า "หมวดชั่วโมงกิจกรรม"
- แสดง **ทุกชุดเกณฑ์แยกส่วน** พร้อมหัวข้อกำกับรุ่น (ดึงจาก GET /criteria-sets):
  - "โครงสร้าง 2567 · นิสิตรหัส 67 ขึ้นไป (ปัจจุบัน ปี 1-3)" → 3 Talent/PLO → รายการ requirement (ชั่วโมง/บังคับ/หน่วยการเรียนรู้/กฎกลุ่ม)
  - "เกณฑ์เดิม · นิสิตรหัส 66 ลงไป (ปัจจุบัน ปี 4)" → 5 หน่วยการเรียนรู้ → หมวดย่อย
  - ป้าย "เกณฑ์ทางการ (อ่านอย่างเดียว)" บนชุด is_system
- ปุ่ม **"+ เพิ่มชุดเกณฑ์ใหม่"** → ฟอร์ม (ชื่อ, ปีรุ่นที่เริ่มใช้, program_type, counting_rule, ชั่วโมงรวม)
- ในชุด custom: เพิ่ม/แก้/ลบ Talent + requirement ได้ (ผูก learning_unit, ชั่วโมง, บังคับ/เลือก, กลุ่มแชร์เป้า)
- หัวข้อแต่ละชุดบอก "ใช้กับนิสิตรหัส XX ขึ้นไป" (จาก effective_from_cohort) + รวมชั่วโมง
- หัวข้อบอกรุ่นด้วย **รหัสนิสิตเป็นหลัก** (คงที่) "(ปัจจุบัน ปี X)" เป็นวงเล็บช่วย

หยุดให้ตรวจ

## ข้อควรระวัง
- แตะ resolver = กระทบการคำนวณ completion/gold ของนิสิตทุกคน → เฟส 1 ต้องเทสละเอียด + ยืนยันว่านิสิต 2,031 คนเดิมยังแมปเข้าชุดเดิมเป๊ะ (68/69 → 2567)
- backup DB ก่อนรัน migration บน Postgres จริง
