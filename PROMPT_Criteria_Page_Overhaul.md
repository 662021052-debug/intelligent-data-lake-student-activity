# Prompt สำหรับ Claude Code — ยกเครื่องหน้า "หมวดชั่วโมง" ให้เนี้ยบ กลมกลืนกับฟอร์มกิจกรรม

**ภาพอ้างอิง:** `ui_mockup_criteria_overhaul.html` (โฟลเดอร์รายงาน)

**คงธีมเทา-ฟ้า + Sarabun + คอมโพเนนต์กลางเดิม · UI ล้วน — ห้ามแตะ backend / resolver / การนับชั่วโมง / completion / gold / ตรรกะช่วงรุ่นที่เพิ่งทำ (cohort_range.dart)**
งานนี้จัดหน้าตาอย่างเดียว ข้อมูลใช้ของเดิมที่ดึงอยู่แล้ว ทำ 2 งาน หยุดให้ตรวจแต่ละงาน · ทุกงาน analyze / `flutter test` (ธรรมดา) / build ต้องผ่าน

---

## งานที่ 1 — จัดการ์ดชุดเกณฑ์ + รายการ requirement ให้เนี้ยบ (`hour_categories_screen.dart`)
> คงป้ายช่วงรุ่น ("67 ขึ้นไป"/"≤66") และบรรทัดชั้นปีที่งานก่อนทำไว้ — แค่จัดวางให้สวยขึ้น

- **หัวการ์ดชุด**: แถบไล่สีอ่อน + เส้นซ้ายน้ำเงิน · เรียง: ป้าย "ชุดที่ N" → ชื่อชุด → **ป้ายช่วงรหัส (เด่น พื้นน้ำเงิน)** → ป้ายล็อก "เกณฑ์ทางการ" ชิดขวา
- **แถบ metric 3 ใบ** ใต้หัว: ชั่วโมงรวม / จำนวนกลุ่ม (Talent หรือหน่วย) / วิธีนับความครบ — การ์ดเล็กพื้นขาว (เหมือน KPI ย่อ)
- **Talent/หน่วย** เป็นแถวพับได้: ▸/▾ + ชื่อ + ป้าย PLO + จำนวนรายการ + **ป้ายผลรวมชั่วโมงของกลุ่ม** ("รวม 30 ชม.") ชิดขวา
- **รายการ requirement**: ชื่อ + บรรทัดย่อย "หน่วยการเรียนรู้: …" + ป้าย **บังคับ (เหลือง)/เลือก (เทา)** + ชั่วโมงชิดขวา · แถวสลับสีอ่อนอ่านง่าย (ใช้ badge/สีชุดเดียวกับฟอร์มกิจกรรม)
- **กฎกลุ่มสังคม ≥16**: แถบเขียวใต้ Talent PLO 3 "กลุ่มสังคม: 2 รายการเลือกรวม ≥ 16 ชม. (ตรวจอัตโนมัติ)"
- คงปุ่มเพิ่ม/แก้/ลบเฉพาะชุด custom (is_system=false) เหมือนเดิม · ชุดทางการยังอ่านอย่างเดียว
- จอแคบ: metric ยุบ, ป้ายไม่ล้น (ทดสอบ 390px)

หยุดให้ฉันตรวจ

---

## งานที่ 2 — ไดอะล็อก "เพิ่มชุดเกณฑ์ใหม่" ให้สไตล์เดียวกับฟอร์มกิจกรรม (`criteria_set_forms.dart`)
ใช้ AppFormDialog + บล็อกหัวข้อ+คำอธิบาย (ตัวเดียวกับที่งานฟอร์มกิจกรรมใช้) — **ไม่เปลี่ยน field ที่ส่ง backend / validation เดิม**

- **บล็อกพระเอก "ชุดนี้ใช้กับนิสิตรุ่นไหน"** (พื้นฟ้ามีกรอบ) วางบนสุด:
  - ช่อง "ปีรุ่นที่เริ่มใช้" + คำอธิบายว่าเป็นตัวกำหนดว่าใครใช้ชุดนี้
  - **พรีวิวสด** (ที่งานก่อนทำไว้) โชว์ในบล็อกนี้: "ชุดนี้จะใช้กับรหัส 70 ขึ้นไป และทำให้ชุด 2567 เหลือ 67–69"
  - **ตรวจว่าพรีวิวขึ้นจริง** — ในภาพผู้ใช้ล่าสุดไม่เห็นบรรทัดพรีวิว ให้ยืนยันว่ามันแสดงเมื่อพิมพ์ปีที่ถูกต้อง (ถ้า regression ให้แก้)
- **บล็อก "ข้อมูลชุดเกณฑ์"**: ชื่อชุด (เต็มแถว) · รหัสชุด + กลุ่มหลักสูตร (2 คอลัมน์) พร้อม help "ห้ามซ้ำ"
- **บล็อก "การนับชั่วโมง"**: ชั่วโมงรวม + วิธีนับความครบ (2 คอลัมน์) พร้อมคำอธิบาย "ต่อรายการ = ต้องครบทุกรายการ · ต่อหน่วย = ดูยอดรวมของหน่วย"
- ทำศัพท์ให้เป็นภาษาคน: dropdown แสดงข้อความไทยเต็ม (ค่าเบื้องหลัง program_type/counting_rule เหมือนเดิม)
- ปุ่มหลัก: "บันทึก แล้วเพิ่มรายการเกณฑ์" (พาไปเพิ่ม talent/requirement ต่อ ตาม flow เดิม)
- คง validation เดิมทั้งหมด: ปีรุ่นซ้ำเตือน, ช่องบังคับ, เตือนผลรวมชั่วโมงไม่เท่า total

หยุดให้ฉันตรวจ

## ข้อควรระวัง
- **UI ล้วน** — payload ที่ส่ง POST/PUT /criteria-sets ต้องเหมือนเดิมทุกคีย์ (code, name, academic_year, effective_from_cohort, program_type, total_required_hours, counting_rule)
- ห้ามแตะ cohort_range.dart (ตรรกะช่วงรุ่น) แค่เรียกใช้
- badge บังคับ/เลือก, สี, การ์ด — ใช้ token/StatusPalette เดียวกับฟอร์มกิจกรรมเพื่อความกลมกลืน
- เทส: `flutter test` ธรรมดา · อัปเดตเทสที่ล็อกข้อความเดิมไว้ · ยืนยันฟอร์มยังส่ง payload เดิม + พรีวิวช่วงรุ่นยังทำงาน
- E2E: เปิดหน้าหมวดชั่วโมง (การ์ด metric + Talent พับได้ + กฎ ≥16) · เปิดไดอะล็อกเพิ่มชุด เห็นบล็อกพระเอก + พรีวิวสด · 390px ไม่ล้น

เริ่มงานที่ 1 ก่อน เสร็จหยุดให้ฉันตรวจ
