# Prompt สำหรับ Claude Code — เปลี่ยนเป็นเมนู Sidebar ซ้าย + หน้าแรกเป็นแดชบอร์ด

คงธีมเทา-ฟ้า ม.ทักษิณ + ฟอนต์ Sarabun + คอมโพเนนต์กลางเดิมทั้งหมด **ไม่รื้อธีม ไม่แตะ business logic**
เปลี่ยนแค่ "โครงนำทาง" จากแถบบน (AppNavBar) เป็น **เมนู sidebar ซ้าย** และทำหน้าแรกเป็นแดชบอร์ด
ทำเป็น 3 งาน หยุดให้ตรวจแต่ละงาน · ทุกงาน analyze/test/build ต้องผ่าน

> **ภาพอ้างอิง**: เปิดไฟล์ `ui_mockups_sidebar_dashboards.html` (ในโฟลเดอร์รายงาน) — มี 2 หน้า:
> แผงควบคุมแอดมิน (sidebar + top bar + KPI ไอคอน + แถบคู่มือ + ภาพรวมสถานะ/งานค้าง) และหน้าแรกนิสิต (แดชบอร์ดส่วนตัว)
>
> **ลำดับกับงาน DB**: งานที่ 1 (sidebar) และงานที่ 2 (แดชบอร์ดแอดมิน) ทำได้เลย ไม่ขึ้นกับ DB ·
> งานที่ 3 (แดชบอร์ดนิสิต) ส่วนที่แบ่งชั่วโมงตาม **Talent/PLO (เกณฑ์ 2567)** ต้องทำ **หลัง** DB rebuild เสร็จ
> ถ้ายังไม่เสร็จ ให้ใช้โครงหมวดเดิมไปก่อนแล้วค่อยสลับเป็น Talent/PLO ทีหลัง

---

## งานที่ 1 — เปลี่ยน AppShell จาก top navbar → sidebar ซ้าย
- **Sidebar ซ้าย** (กว้าง ~210px) พื้นน้ำเงินเข้ม (ใช้ค่าฟ้าเข้มจากธีม เช่น #155189/#16548C) ตัวอักษรขาว/ฟ้าอ่อน:
  - บนสุด: โลโก้ TSU + ชื่อระบบ
  - หัวข้อกลุ่ม "เมนูผู้ดูแลระบบ / เมนูนิสิต" แล้วตามด้วยรายการเมนู (ไอคอน + ข้อความ)
  - เมนูที่มีหน้าย่อยให้ **พับ/กางได้** (เช่น แดชบอร์ด → ภาพรวม, สถิติ) — ตัวที่เปิดอยู่ไฮไลต์พื้นขาวตัวน้ำเงิน
  - ล่างสุด: avatar + ชื่อ + ปุ่มออกจากระบบ
- **แถบบนบาง** เหนือเนื้อหา (พื้นฟ้า #1C6EB4 ตัวขาว): ปุ่มพับ sidebar (hamburger) + ชื่อหน้า/ระบบ + กระดิ่งแจ้งเตือน + ชื่อผู้ใช้ + ป้ายบทบาท (pill ขาว)
- เมนูต่างกันตาม role เหมือนเดิม (navItemsForRole) · เมนูต้องพาไปทุกหน้าได้เหมือนเดิม
- **จอแคบ**: sidebar ยุบซ่อน กดปุ่ม hamburger เพื่อเลื่อนออกมา (drawer) — ทดสอบที่ 360px ต้องไม่ล้น
- ปรับ AppShell ที่มีอยู่ให้เปลี่ยนโครง โดยหน้าที่ห่อด้วย AppShell อยู่แล้วไม่ต้องแก้ทีละหน้า

หยุดให้ฉันตรวจ

---

## งานที่ 2 — หน้าแรกแอดมิน = แดชบอร์ดคอนโซล
ให้ role=admin เข้ามาเจอแดชบอร์ดทันที (แทนแผงการ์ดเมนูเดิม) ประกอบด้วย:
- หัวข้อ "Dashboard" + คำอธิบาย + เวลาอัปเดต + แถวปุ่มลัด (จัดการกิจกรรม/ผู้ใช้/หมวดชั่วโมง/รีเฟรช Gold)
- **แถบคู่มือ** (การ์ดพื้นฟ้าเข้ม) ปุ่ม "เปิดคู่มือ" — ถ้ายังไม่มีเนื้อหาคู่มือ ให้ลิงก์ไป README/หน้าเปล่าไว้ก่อน อย่าเป็นปุ่มหลอกที่ไม่ทำอะไร (ซ่อนไปก่อนก็ได้ถ้ายังไม่มีปลายทาง)
- **KPI 4 ใบ มีไอคอนวงกลม** (ใช้ KpiCard เดิม เพิ่มช่องไอคอน): นิสิตทั้งหมด/ผ่านเกณฑ์ %/กลุ่มเสี่ยง/จำนวนกิจกรรม
- กราฟแนวโน้ม + ตารางกลุ่มเสี่ยง (ย้ายจากหน้าแดชบอร์ดเดิมมาไว้ที่นี่ — เป็นหน้าเดียวกัน)
- 2 การ์ดสรุป: "ภาพรวมสถานะ" (รออนุมัติกิจกรรม/หลักฐานรอตรวจ/อนุมัติวันนี้) + "งานที่ต้องดำเนินการ" (ลิงก์ไปหน้าที่เกี่ยว)
- ใช้ข้อมูลจาก endpoint dashboard ที่มีอยู่แล้ว ห้ามเพิ่ม backend
- เมนู sidebar "แดชบอร์ด" ชี้หน้านี้ · แผงการ์ดเมนูเดิมเอาออก/ย้ายเป็นปุ่มลัด อย่าให้มีหน้ากำพร้า

หยุดให้ฉันตรวจ

---

## งานที่ 3 — หน้าแรกนิสิต = แดชบอร์ดส่วนตัว
- แถวต้อนรับ "สวัสดี, {ชื่อ}" + รหัส/คณะ/ชั้นปี
- **KPI 4 ใบ**: ชั่วโมงสะสม X/60 · % ครบเกณฑ์ · หมวดที่ยังไม่ครบ · กิจกรรมที่กำลังจะถึง
- **ชั่วโมงสะสมแยกหมวด** (ProgressRow เดิม)
- การ์ด "แนะนำให้เข้าร่วม" (กิจกรรมเปิดรับในหมวดที่ยังขาด / ถ้าไม่มี logic ใช้ upcoming เรียงใกล้สุด)
- ปุ่ม "ใบประมวลผลกิจกรรม" คงไว้ (พาไปรายละเอียดชั่วโมง)
- ตาราง "กิจกรรมที่เข้าร่วม" เดิม → ย้ายไปหน้ารายละเอียดชั่วโมง (ไม่ให้ข้อมูลหาย)
- ใช้ข้อมูลโปรไฟล์/hours-summary ที่ดึงอยู่แล้ว · fallback ไม่พังทั้งหน้า · จอแคบ KPI ยุบ 4→2→1

หยุดให้ฉันตรวจ

## เริ่มงานที่ 1 (sidebar shell) ก่อน เสร็จหยุดให้ฉันตรวจ
โครงหน้าตามที่อนุมัติใน mockup (แดชบอร์ดแอดมิน sidebar + แดชบอร์ดนิสิต) — ธีมสีเทา-ฟ้าคงเดิม
