# Prompt สำหรับ Claude Code — ยกเครื่อง UI ทั้งแอป (ธีม ม.ทักษิณ เทา-ฟ้า)

เป้าหมาย: ปรับ UI ทั้งแอปให้เข้าธีมเดียวกันตาม mockup ที่อนุมัติแล้ว (ไฟล์ `ui_mockups_tsu_theme.html`
ในโฟลเดอร์รายงาน — เปิดดูเป็นภาพอ้างอิงก่อนเริ่ม) **ไม่เปลี่ยนฟังก์ชัน/logic/ข้อความ** ทำเป็นเฟส หยุดให้ตรวจทุกเฟส

## กติกาตลอดงาน
- แก้เฉพาะหน้าตา ห้ามแตะ business logic / endpoint / โครงสร้างข้อมูล
- ทุกเฟสจบด้วย `flutter analyze` สะอาด + `flutter test` เขียว + `flutter build web` ผ่าน
- widget test พังเพราะข้อความ/โครงเปลี่ยน → แก้เทสเฉพาะที่ตั้งใจเปลี่ยนจริง ห้ามแก้เพื่อกลบของพัง
- คงข้อความไทยและความหมายเดิม รองรับจอกว้าง + จอแคบ

---

## Design tokens (ล็อกค่าตามนี้ — ทำเป็น ThemeData กลางที่ `lib/theme/app_theme.dart`)

**สี (ธีมประจำ ม.ทักษิณ: เทา-ฟ้า)**
- ฟ้าหลัก (primary/brand): `#1C6EB4` · ฟ้าเข้ม (hover/nav active): `#155189` · ฟ้าอ่อน (พื้นอ่อน): `#E8F1FA`
- เทา (secondary/neutral): `#5B6470` · ตัวอักษรหลัก `#222831` · รอง `#5B6470` · จาง `#98A0AB`
- เส้น `#E3E7EC` · พื้นหน้า (page) `#EEF1F4` · การ์ด/พื้นผิว `#FFFFFF`
- สถานะ: สำเร็จ/ผ่าน `#1F9D57` (พื้น `#E4F5EC`) · รออนุมัติ `#E0902B` (พื้น `#FCF1DE`) · ปฏิเสธ/ลบ `#D6453D` (พื้น `#FBE7E6`)

**ฟอนต์**: เพิ่ม `google_fonts` แล้วใช้ **Sarabun** ทั้งแอป (น้ำหนัก 400/500/600/700)
ตั้ง Material 3 (`useMaterial3: true`) + `ColorScheme.fromSeed(seedColor: Color(0xFF1C6EB4))`

**รูปทรง/ระยะห่าง**: การ์ดมุมโค้ง 12–14, ปุ่ม/ช่องกรอก 8, เงาการ์ดบางๆ · ระยะห่างใช้สเกล 4/8/16/24

---

## คอมโพเนนต์กลาง (ทำให้ทุกหน้าหยิบใช้ หน้าตาตาม mockup)
- **Top NavBar**: โลโก้ TSU (กล่องฟ้า ตัวอักษรขาว "TSU") + ชื่อระบบ + เมนูแนวนอนตรงกลาง
  (เมนูที่เปิดอยู่ = ฟ้า ขีดเส้นใต้) + avatar + ออกจากระบบ — เมนูต่างกันตาม role (นิสิต/เจ้าหน้าที่/แอดมิน)
- **ปุ่ม**: หลัก=พื้นฟ้า ตัวขาว · เข้ม=`#2A2F3A` (เช่นใบประมวลผล) · รอง=ขอบเทาพื้นขาว
- **status chip (pill)**: เขียว=อนุมัติ/ผ่าน, เหลือง=รออนุมัติ, แดง=ปฏิเสธ, เทา=ซ่อน — ตามสีสถานะด้านบน
- **ตาราง**: หัวตารางตัวเล็กสีจาง เส้นแบ่งบาง แถวโปร่ง อ่านง่าย (แทน DataTable เปล่า)
- **การ์ดรายการ / การ์ดกิจกรรม / metric card**, **ปุ่มไอคอน** (แก้/ลบ/QR) กล่องขอบเทา
- **field ค้นหา/กรอกข้อมูล** พื้นเทาอ่อน ขอบบาง · **empty/loading state** มีไอคอน + ข้อความไทย

---

## เฟสการทำงาน (หยุดให้ตรวจทุกเฟส อย่าทำรวด)
1. **เฟส 1** — ThemeData กลาง + ฟอนต์ Sarabun + คอมโพเนนต์กลาง (NavBar, ปุ่ม, chip, ตาราง, การ์ด) — แค่นี้แอปควรเปลี่ยนทั้งระบบ
2. **เฟส 2** — หน้านิสิต: หน้าแรก (โปรไฟล์+แถบชั่วโมงแยกหมวด), สมัครกิจกรรม, แดชบอร์ดชั่วโมง, chatbot, เช็กอิน QR
3. **เฟส 3** — เจ้าหน้าที่/แอดมิน: จัดการกิจกรรม, จัดการผู้ใช้/นิสิต, หมวดชั่วโมง, นำเข้าแผน
4. **เฟส 4** — แดชบอร์ดผู้บริหาร (KPI card + กราฟให้เข้าธีมฟ้า) + ตรวจรวม responsive/contrast

อ้างอิงหน้าตาแต่ละหน้าจากไฟล์ `ui_mockups_tsu_theme.html` (10 หน้า)

## เริ่มเฟส 1 ก่อน เสร็จหยุดให้ฉันตรวจ
