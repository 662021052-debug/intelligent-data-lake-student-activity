# Prompt สำหรับ Claude Code — ปลดล็อกให้แก้ไขได้ทั้ง 2 ชุดเกณฑ์ (2567 + เกณฑ์เดิม)

**นี่เป็น behavior change (แตะ backend gating + seed + UI) ไม่ใช่ UI ล้วน** ทำเป็นเฟส หยุดให้ตรวจ · ทุกเฟส test/build ต้องผ่าน

## บริบท
ตอนนี้ชุด `legacy` (≤66) แอดมินแก้/เพิ่ม/ลบ Talent-requirement ได้ แต่ชุด `2567-regular` ถูกล็อก "อ่านอย่างเดียว"
ผู้ใช้ต้องการให้ **ทั้ง 2 ชุดแก้ไขได้เหมือนกัน**

## ⚠️ 2 จุดเสี่ยงที่ต้องทำให้ถูก (สำคัญ)
1. **seed เขียนทับ**: เดิมชุด is_system=true ถูก `seed_criteria.py` re-enforce ตอน boot ถ้าปลดล็อกให้แก้ แต่ seed ยังเขียนทับ → **แอดมินแก้แล้วหายตอน restart** → ต้องทำให้ seed **ไม่เขียนทับชุดที่มีอยู่แล้ว** (สร้างใหม่เฉพาะตอนยังไม่มีในฐานข้อมูล)
2. **กระทบการตัดสินนิสิต**: 2567 ใช้คำนวณ completion ของนิสิตปี 1–3 อยู่จริง → **ห้ามแตะ resolver / completion / gold / การนับชั่วโมง** แค่เปิดให้แก้โครงเกณฑ์ผ่าน CRUD เดิมที่ legacy ใช้อยู่

---

## เฟส 1 — backend: เปิดสิทธิ์แก้ + กัน seed เขียนทับ
- ทำให้ CRUD ของ Talent/requirement/requirement_group ที่ legacy ใช้ได้ **ใช้กับชุด 2567 ได้ด้วย** (เอา guard ที่บล็อก is_system=true ออก หรือปรับให้ทั้งสองชุดผ่าน)
- **`seed_criteria.py`**: เปลี่ยนจาก "re-enforce/overwrite ชุด is_system=true" เป็น **"สร้างเฉพาะเมื่อยังไม่มีชุดนั้น (by code) เท่านั้น ถ้ามีแล้วไม่แตะ"** — กันแก้หาย
  - ยืนยันด้วยเทส: มีชุด 2567 อยู่ + แก้ requirement แล้วรัน seed ซ้ำ → ค่าที่แก้ยังอยู่ ไม่ถูกเขียนทับ
- ลบ/ลดการพึ่ง `is_system` เฉพาะส่วน "อ่านอย่างเดียว" — **แต่คงป้าย/ความหมายชุดทางการไว้ได้** (แค่ไม่บังคับ read-only) ถ้าจะเก็บ is_system เป็นแค่ label ก็ได้
- **ห้ามแตะ**: criteria.py resolver, completion.py, gold.py, approval.py, cohort_range
- เทส: แก้ requirement ของ 2567 ได้ (200), seed ไม่ทับ, completion ของนิสิตยังคำนวณเหมือนเดิมเมื่อโครงไม่เปลี่ยน

หยุดให้ฉันตรวจ

## เฟส 2 — frontend: ใส่ปุ่มแก้/เพิ่ม/ลบให้ชุด 2567 เหมือนเกณฑ์เดิม
- ในหน้าหมวดชั่วโมง (งานที่เพิ่งทำ): ชุด 2567 แสดงปุ่ม เพิ่ม/แก้/ลบ Talent-requirement เหมือน legacy
- **เอาป้าย "🔒 อ่านอย่างเดียว" ออกจากทั้ง 2 ชุด** (เพราะแก้ได้ทั้งคู่แล้ว)
- ถ้าจะคงคำว่า "เกณฑ์ทางการ" ไว้เป็น label ได้ แต่ไม่สื่อว่าแก้ไม่ได้
- ปุ่ม/ฟอร์มแก้ใช้ตัวเดียวกับที่ legacy ใช้ (ไม่สร้างใหม่)

หยุดให้ฉันตรวจ

## เฟส 3 — ยืนยันปลอดภัย
- **ทดสอบเด็ดขาด: แก้ requirement ของ 2567 → restart backend (docker compose up -d) → ค่าที่แก้ต้องยังอยู่** (พิสูจน์ว่า seed ไม่ทับ)
- ยืนยัน completion ของนิสิตตัวอย่างไม่เปลี่ยนถ้าไม่ได้แก้โครง (regression)
- `flutter test` ธรรมดา + backend test ที่เกี่ยวข้อง ผ่าน
- E2E: เปิด 2567 เห็นปุ่มแก้ได้, ไม่มีป้ายล็อก, 390px ไม่ล้น

## ข้อควรระวังรวม
- อย่าลืมว่าถ้าแก้ requirement ที่นิสิตสมัคร/ได้ชั่วโมงไปแล้วผูกอยู่ อาจมีผลกับ snapshot — ให้รายงานถ้าเจอจุดนี้ (ยังไม่ต้องแก้ แค่เตือน)
- backup DB ก่อนถ้าทดสอบบน Postgres จริง

เริ่มเฟส 1 ก่อน เสร็จหยุดให้ฉันตรวจ
