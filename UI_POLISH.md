# สเปกปรับ UI ทั้งระบบ สำหรับ Claude Code — Flutter Web

> วิธีใช้: วางไฟล์นี้ในโฟลเดอร์โปรเจกต์ แล้วสั่ง Claude Code:
> "อ่าน UI_POLISH.md แล้วทำทีละกลุ่ม เริ่มกลุ่ม 1 (ธีม) ก่อน หยุดให้ฉันดูก่อนไปต่อ"
>
> เป้าหมาย: ทำให้ดูเป็นมืออาชีพ กลมกลืน และสม่ำเสมอทั้งแอป โดย**ไม่เปลี่ยน logic/flow เดิม** แก้เฉพาะชั้นการแสดงผล · หลังแก้ต้อง `flutter analyze` สะอาด + widget test ผ่าน

---

## กลุ่ม 1 — ธีมรวม (ทำก่อน สำคัญสุด — ได้ผลมากสุดต่อความสวย)

1. ใช้ **Material 3** (`useMaterial3: true`) + `ColorScheme.fromSeed` สีหลักเดียว (indigo/ม่วง ~ `#4F46E5` ให้เข้ากับที่ใช้อยู่) กำหนดใน `ThemeData` ที่ `main.dart` ที่เดียว ทุกหน้าดึงจากธีม
2. **ฟอนต์ไทยที่อ่านง่าย** — ใช้ `google_fonts` เช่น **Noto Sans Thai** หรือ **Sarabun** ตั้งเป็น default text theme (ตัวเลขไทย/อังกฤษคมขึ้นมาก)
3. กำหนดค่ากลางให้สม่ำเสมอทั้งแอป: `cardTheme` (มุมโค้ง 12–16, เงาบาง), `inputDecorationTheme` (outlined, มุมโค้ง), `chipTheme`, ระยะห่างมาตรฐาน (8/12/16/24)
4. สถานะใช้สีสื่อความหมายสม่ำเสมอทั้งระบบ: อนุมัติ=เขียว, รอตรวจ=เหลือง/ส้ม, ไม่อนุมัติ=แดง, pending กิจกรรม=เทา — ทำเป็น widget `StatusChip` ตัวเดียวใช้ซ้ำ (เลิก hardcode สีในแต่ละหน้า)

## กลุ่ม 2 — หน้า Home + เมนู

1. เพิ่ม **header ต้อนรับ**: "สวัสดี, {ชื่อ}" + ป้าย role (นิสิต/เจ้าหน้าที่/ผู้ดูแล) มุมขวามีปุ่มออกจากระบบชัดเจน
2. การ์ดเมนูกลางจอ → จัดเป็น **grid ที่ responsive** (จอเล็ก 1–2 คอลัมน์, จอใหญ่ 3–4) การ์ดมีไอคอนใหญ่ + ชื่อ + คำอธิบายสั้น + hover effect
3. **ฝั่งนิสิต**: เพิ่มการ์ดสรุปเล็ก ๆ ด้านบน "ชั่วโมงสะสม X/60" + แถบ progress — เห็นสถานะตัวเองทันทีที่เข้า
4. แก้ถ้อยคำฝั่งนิสิตที่ยังเขียน "**จัดการ**กิจกรรม/จัดการข้อมูลกิจกรรม" → เป็น "กิจกรรม / ดูกิจกรรมทั้งหมด" (นิสิตดูอย่างเดียว)

## กลุ่ม 3 — ตาราง / ฟอร์ม / dialog

1. **แก้ dialog แก้ไขกิจกรรม (บั๊กเดิม)**: ช่อง "หมวดชั่วโมง" ข้อความล้นออกนอกกรอบ + ยังไม่ใช่ dropdown → ทำเป็น **DropdownButtonFormField** เลือก subcategory แสดงชื่อสั้น ไม่ล้น
2. **ลบปุ่ม "Back" ที่ซ้ำ** มุมซ้ายบน (มีลูกศร ← อยู่แล้ว) — เดิมแจ้งหลายรอบยังไม่แก้
3. ตาราง: หัวตารางตัวหนา, แถวสลับสีอ่อน (zebra) หรือเส้นคั่นบาง, hover highlight, สถานะใช้ `StatusChip`, ปุ่มไอคอนมี tooltip
4. **Empty state**: หน้าที่ไม่มีข้อมูลให้แสดงไอคอน + ข้อความ ("ยังไม่มีรายการ") แทนตารางว่างเปล่า
5. dialog ทุกตัว: ขนาด/ระยะขอบสม่ำเสมอ, ปุ่มยกเลิก/บันทึกจัดชิดขวา, ปุ่มบันทึกเป็น primary
6. ปุ่มยืนยันการทำรายการสำคัญ (สมัคร/ยกเลิก/ลบ) แสดงข้อความสรุปชัดก่อนกด

## กลุ่ม 4 — แดชบอร์ด + แชตบอต

**แดชบอร์ด (admin):**
1. **KPI cards** แถวบน: ไอคอน + ตัวเลขใหญ่ + label + สีตามความหมาย (% ผ่านเกณฑ์ ถ้าต่ำให้เป็นสีเตือน)
2. กราฟ `fl_chart`: ใส่สีตามธีม, label แกนอ่านง่าย, legend, tooltip ตอน hover
3. ตารางกลุ่มเสี่ยง: เน้นแถวสีแดงอ่อน + แสดง % ที่ยังขาด, คลิกไปดูรายคนได้
4. filter (คณะ/ชั้นปี/ภาคเรียน) จัดเป็นแถบบนสวยงาม

**แชตบอต (นิสิต):**
5. ฟองข้อความ: ผู้ใช้ชิดขวา (สีหลัก) / บอทชิดซ้าย (สีอ่อน) มุมโค้ง, มี avatar บอท
6. typing indicator ตอนรอคำตอบ, auto-scroll ลงล่างสุด
7. quick prompts เป็น chip กดง่าย, ถ้าคำตอบอ้างกิจกรรมให้มีปุ่ม "ไปสมัคร"
8. จัด layout ให้ช่องพิมพ์ติดล่าง หน้าจอไม่กระโดด

---

## หลักที่ต้องยึดตลอด
- **responsive** ทุกหน้า (LayoutBuilder/Wrap/GridView) ดูดีทั้งมือถือและจอใหญ่
- **ไม่แตะ logic/permission/flow** — แก้แค่การแสดงผล
- ทำ component ใช้ซ้ำ (`StatusChip`, การ์ด KPI, ฟองแชต) แทนก็อปสไตล์ซ้ำในแต่ละหน้า
- หลังแต่ละกลุ่ม: `flutter analyze` ต้องสะอาด, test ผ่าน, แล้ว commit แยกเป็นกลุ่ม
